<?php
/*
 * Copyright 2021 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

/*
 * GENERATED CODE WARNING
 * Generated by gapic-generator-php from the file
 * https://github.com/googleapis/googleapis/blob/master/google/monitoring/metricsscope/v1/metrics_scopes.proto
 * Updates to the above are reflected here through a refresh process.
 */

namespace Google\Cloud\Monitoring\MetricsScope\V1\Gapic;

use Google\ApiCore\ApiException;
use Google\ApiCore\CredentialsWrapper;
use Google\ApiCore\GapicClientTrait;

use Google\ApiCore\LongRunning\OperationsClient;
use Google\ApiCore\OperationResponse;

use Google\ApiCore\PathTemplate;
use Google\ApiCore\RequestParamsHeaderDescriptor;
use Google\ApiCore\RetrySettings;
use Google\ApiCore\Transport\TransportInterface;
use Google\ApiCore\ValidationException;
use Google\Auth\FetchAuthTokenInterface;
use Google\Cloud\Monitoring\MetricsScope\V1\CreateMonitoredProjectRequest;
use Google\Cloud\Monitoring\MetricsScope\V1\DeleteMonitoredProjectRequest;
use Google\Cloud\Monitoring\MetricsScope\V1\GetMetricsScopeRequest;
use Google\Cloud\Monitoring\MetricsScope\V1\ListMetricsScopesByMonitoredProjectRequest;
use Google\Cloud\Monitoring\MetricsScope\V1\ListMetricsScopesByMonitoredProjectResponse;
use Google\Cloud\Monitoring\MetricsScope\V1\MetricsScope;
use Google\Cloud\Monitoring\MetricsScope\V1\MonitoredProject;
use Google\LongRunning\Operation;

/**
 * Service Description: Manages Cloud Monitoring Metrics Scopes, and the monitoring of Google Cloud
 * projects and AWS accounts.
 *
 * This class provides the ability to make remote calls to the backing service through method
 * calls that map to API methods. Sample code to get started:
 *
 * ```
 * $metricsScopesClient = new MetricsScopesClient();
 * try {
 *     $formattedParent = $metricsScopesClient->metricsScopeName('[METRICS_SCOPE]');
 *     $monitoredProject = new MonitoredProject();
 *     $operationResponse = $metricsScopesClient->createMonitoredProject($formattedParent, $monitoredProject);
 *     $operationResponse->pollUntilComplete();
 *     if ($operationResponse->operationSucceeded()) {
 *         $result = $operationResponse->getResult();
 *     // doSomethingWith($result)
 *     } else {
 *         $error = $operationResponse->getError();
 *         // handleError($error)
 *     }
 *     // Alternatively:
 *     // start the operation, keep the operation name, and resume later
 *     $operationResponse = $metricsScopesClient->createMonitoredProject($formattedParent, $monitoredProject);
 *     $operationName = $operationResponse->getName();
 *     // ... do other work
 *     $newOperationResponse = $metricsScopesClient->resumeOperation($operationName, 'createMonitoredProject');
 *     while (!$newOperationResponse->isDone()) {
 *         // ... do other work
 *         $newOperationResponse->reload();
 *     }
 *     if ($newOperationResponse->operationSucceeded()) {
 *         $result = $newOperationResponse->getResult();
 *     // doSomethingWith($result)
 *     } else {
 *         $error = $newOperationResponse->getError();
 *         // handleError($error)
 *     }
 * } finally {
 *     $metricsScopesClient->close();
 * }
 * ```
 *
 * Many parameters require resource names to be formatted in a particular way. To
 * assist with these names, this class includes a format method for each type of
 * name, and additionally a parseName method to extract the individual identifiers
 * contained within formatted names that are returned by the API.
 */
class MetricsScopesGapicClient
{
    use GapicClientTrait;

    /**
     * The name of the service.
     */
    const SERVICE_NAME = 'google.monitoring.metricsscope.v1.MetricsScopes';

    /**
     * The default address of the service.
     */
    const SERVICE_ADDRESS = 'monitoring.googleapis.com';

    /**
     * The default port of the service.
     */
    const DEFAULT_SERVICE_PORT = 443;

    /**
     * The name of the code generator, to be included in the agent header.
     */
    const CODEGEN_NAME = 'gapic';

    /**
     * The default scopes required by the service.
     */
    public static $serviceScopes = [
        'https://www.googleapis.com/auth/cloud-platform',
        'https://www.googleapis.com/auth/monitoring',
        'https://www.googleapis.com/auth/monitoring.read',
        'https://www.googleapis.com/auth/monitoring.write',
    ];

    private static $metricsScopeNameTemplate;

    private static $monitoredProjectNameTemplate;

    private static $pathTemplateMap;

    private $operationsClient;

    private static function getClientDefaults()
    {
        return [
            'serviceName' => self::SERVICE_NAME,
            'serviceAddress' => self::SERVICE_ADDRESS . ':' . self::DEFAULT_SERVICE_PORT,
            'clientConfig' => __DIR__ . '/../resources/metrics_scopes_client_config.json',
            'descriptorsConfigPath' => __DIR__ . '/../resources/metrics_scopes_descriptor_config.php',
            'gcpApiConfigPath' => __DIR__ . '/../resources/metrics_scopes_grpc_config.json',
            'credentialsConfig' => [
                'defaultScopes' => self::$serviceScopes,
            ],
            'transportConfig' => [
                'rest' => [
                    'restClientConfigPath' => __DIR__ . '/../resources/metrics_scopes_rest_client_config.php',
                ],
            ],
        ];
    }

    private static function getMetricsScopeNameTemplate()
    {
        if (self::$metricsScopeNameTemplate == null) {
            self::$metricsScopeNameTemplate = new PathTemplate('locations/global/metricsScope/{metrics_scope}');
        }

        return self::$metricsScopeNameTemplate;
    }

    private static function getMonitoredProjectNameTemplate()
    {
        if (self::$monitoredProjectNameTemplate == null) {
            self::$monitoredProjectNameTemplate = new PathTemplate('locations/global/metricsScopes/{metrics_scope}/projects/{project}');
        }

        return self::$monitoredProjectNameTemplate;
    }

    private static function getPathTemplateMap()
    {
        if (self::$pathTemplateMap == null) {
            self::$pathTemplateMap = [
                'metricsScope' => self::getMetricsScopeNameTemplate(),
                'monitoredProject' => self::getMonitoredProjectNameTemplate(),
            ];
        }

        return self::$pathTemplateMap;
    }

    /**
     * Formats a string containing the fully-qualified path to represent a
     * metrics_scope resource.
     *
     * @param string $metricsScope
     *
     * @return string The formatted metrics_scope resource.
     */
    public static function metricsScopeName($metricsScope)
    {
        return self::getMetricsScopeNameTemplate()->render([
            'metrics_scope' => $metricsScope,
        ]);
    }

    /**
     * Formats a string containing the fully-qualified path to represent a
     * monitored_project resource.
     *
     * @param string $metricsScope
     * @param string $project
     *
     * @return string The formatted monitored_project resource.
     */
    public static function monitoredProjectName($metricsScope, $project)
    {
        return self::getMonitoredProjectNameTemplate()->render([
            'metrics_scope' => $metricsScope,
            'project' => $project,
        ]);
    }

    /**
     * Parses a formatted name string and returns an associative array of the components in the name.
     * The following name formats are supported:
     * Template: Pattern
     * - metricsScope: locations/global/metricsScope/{metrics_scope}
     * - monitoredProject: locations/global/metricsScopes/{metrics_scope}/projects/{project}
     *
     * The optional $template argument can be supplied to specify a particular pattern,
     * and must match one of the templates listed above. If no $template argument is
     * provided, or if the $template argument does not match one of the templates
     * listed, then parseName will check each of the supported templates, and return
     * the first match.
     *
     * @param string $formattedName The formatted name string
     * @param string $template      Optional name of template to match
     *
     * @return array An associative array from name component IDs to component values.
     *
     * @throws ValidationException If $formattedName could not be matched.
     */
    public static function parseName($formattedName, $template = null)
    {
        $templateMap = self::getPathTemplateMap();
        if ($template) {
            if (!isset($templateMap[$template])) {
                throw new ValidationException("Template name $template does not exist");
            }

            return $templateMap[$template]->match($formattedName);
        }

        foreach ($templateMap as $templateName => $pathTemplate) {
            try {
                return $pathTemplate->match($formattedName);
            } catch (ValidationException $ex) {
                // Swallow the exception to continue trying other path templates
            }
        }

        throw new ValidationException("Input did not match any known format. Input: $formattedName");
    }

    /**
     * Return an OperationsClient object with the same endpoint as $this.
     *
     * @return OperationsClient
     */
    public function getOperationsClient()
    {
        return $this->operationsClient;
    }

    /**
     * Resume an existing long running operation that was previously started by a long
     * running API method. If $methodName is not provided, or does not match a long
     * running API method, then the operation can still be resumed, but the
     * OperationResponse object will not deserialize the final response.
     *
     * @param string $operationName The name of the long running operation
     * @param string $methodName    The name of the method used to start the operation
     *
     * @return OperationResponse
     */
    public function resumeOperation($operationName, $methodName = null)
    {
        $options = isset($this->descriptors[$methodName]['longRunning']) ? $this->descriptors[$methodName]['longRunning'] : [];
        $operation = new OperationResponse($operationName, $this->getOperationsClient(), $options);
        $operation->reload();
        return $operation;
    }

    /**
     * Constructor.
     *
     * @param array $options {
     *     Optional. Options for configuring the service API wrapper.
     *
     *     @type string $serviceAddress
     *           The address of the API remote host. May optionally include the port, formatted
     *           as "<uri>:<port>". Default 'monitoring.googleapis.com:443'.
     *     @type string|array|FetchAuthTokenInterface|CredentialsWrapper $credentials
     *           The credentials to be used by the client to authorize API calls. This option
     *           accepts either a path to a credentials file, or a decoded credentials file as a
     *           PHP array.
     *           *Advanced usage*: In addition, this option can also accept a pre-constructed
     *           {@see \Google\Auth\FetchAuthTokenInterface} object or
     *           {@see \Google\ApiCore\CredentialsWrapper} object. Note that when one of these
     *           objects are provided, any settings in $credentialsConfig will be ignored.
     *     @type array $credentialsConfig
     *           Options used to configure credentials, including auth token caching, for the
     *           client. For a full list of supporting configuration options, see
     *           {@see \Google\ApiCore\CredentialsWrapper::build()} .
     *     @type bool $disableRetries
     *           Determines whether or not retries defined by the client configuration should be
     *           disabled. Defaults to `false`.
     *     @type string|array $clientConfig
     *           Client method configuration, including retry settings. This option can be either
     *           a path to a JSON file, or a PHP array containing the decoded JSON data. By
     *           default this settings points to the default client config file, which is
     *           provided in the resources folder.
     *     @type string|TransportInterface $transport
     *           The transport used for executing network requests. May be either the string
     *           `rest` or `grpc`. Defaults to `grpc` if gRPC support is detected on the system.
     *           *Advanced usage*: Additionally, it is possible to pass in an already
     *           instantiated {@see \Google\ApiCore\Transport\TransportInterface} object. Note
     *           that when this object is provided, any settings in $transportConfig, and any
     *           $serviceAddress setting, will be ignored.
     *     @type array $transportConfig
     *           Configuration options that will be used to construct the transport. Options for
     *           each supported transport type should be passed in a key for that transport. For
     *           example:
     *           $transportConfig = [
     *               'grpc' => [...],
     *               'rest' => [...],
     *           ];
     *           See the {@see \Google\ApiCore\Transport\GrpcTransport::build()} and
     *           {@see \Google\ApiCore\Transport\RestTransport::build()} methods for the
     *           supported options.
     *     @type callable $clientCertSource
     *           A callable which returns the client cert as a string. This can be used to
     *           provide a certificate and private key to the transport layer for mTLS.
     * }
     *
     * @throws ValidationException
     */
    public function __construct(array $options = [])
    {
        $clientOptions = $this->buildClientOptions($options);
        $this->setClientOptions($clientOptions);
        $this->operationsClient = $this->createOperationsClient($clientOptions);
    }

    /**
     * Adds a `MonitoredProject` with the given project ID
     * to the specified `Metrics Scope`.
     *
     * Sample code:
     * ```
     * $metricsScopesClient = new MetricsScopesClient();
     * try {
     *     $formattedParent = $metricsScopesClient->metricsScopeName('[METRICS_SCOPE]');
     *     $monitoredProject = new MonitoredProject();
     *     $operationResponse = $metricsScopesClient->createMonitoredProject($formattedParent, $monitoredProject);
     *     $operationResponse->pollUntilComplete();
     *     if ($operationResponse->operationSucceeded()) {
     *         $result = $operationResponse->getResult();
     *     // doSomethingWith($result)
     *     } else {
     *         $error = $operationResponse->getError();
     *         // handleError($error)
     *     }
     *     // Alternatively:
     *     // start the operation, keep the operation name, and resume later
     *     $operationResponse = $metricsScopesClient->createMonitoredProject($formattedParent, $monitoredProject);
     *     $operationName = $operationResponse->getName();
     *     // ... do other work
     *     $newOperationResponse = $metricsScopesClient->resumeOperation($operationName, 'createMonitoredProject');
     *     while (!$newOperationResponse->isDone()) {
     *         // ... do other work
     *         $newOperationResponse->reload();
     *     }
     *     if ($newOperationResponse->operationSucceeded()) {
     *         $result = $newOperationResponse->getResult();
     *     // doSomethingWith($result)
     *     } else {
     *         $error = $newOperationResponse->getError();
     *         // handleError($error)
     *     }
     * } finally {
     *     $metricsScopesClient->close();
     * }
     * ```
     *
     * @param string           $parent           Required. The resource name of the existing `Metrics Scope` that will monitor this
     *                                           project.
     *                                           Example:
     *                                           `locations/global/metricsScopes/{SCOPING_PROJECT_ID_OR_NUMBER}`
     * @param MonitoredProject $monitoredProject Required. The initial `MonitoredProject` configuration.
     *                                           Specify only the `monitored_project.name` field. All other fields are
     *                                           ignored. The `monitored_project.name` must be in the format:
     *                                           `locations/global/metricsScopes/{SCOPING_PROJECT_ID_OR_NUMBER}/projects/{MONITORED_PROJECT_ID_OR_NUMBER}`
     * @param array            $optionalArgs     {
     *     Optional.
     *
     *     @type RetrySettings|array $retrySettings
     *           Retry settings to use for this call. Can be a
     *           {@see Google\ApiCore\RetrySettings} object, or an associative array of retry
     *           settings parameters. See the documentation on
     *           {@see Google\ApiCore\RetrySettings} for example usage.
     * }
     *
     * @return \Google\ApiCore\OperationResponse
     *
     * @throws ApiException if the remote call fails
     */
    public function createMonitoredProject($parent, $monitoredProject, array $optionalArgs = [])
    {
        $request = new CreateMonitoredProjectRequest();
        $requestParamHeaders = [];
        $request->setParent($parent);
        $request->setMonitoredProject($monitoredProject);
        $requestParamHeaders['parent'] = $parent;
        $requestParams = new RequestParamsHeaderDescriptor($requestParamHeaders);
        $optionalArgs['headers'] = isset($optionalArgs['headers']) ? array_merge($requestParams->getHeader(), $optionalArgs['headers']) : $requestParams->getHeader();
        return $this->startOperationsCall('CreateMonitoredProject', $optionalArgs, $request, $this->getOperationsClient())->wait();
    }

    /**
     * Deletes a `MonitoredProject` from the specified `Metrics Scope`.
     *
     * Sample code:
     * ```
     * $metricsScopesClient = new MetricsScopesClient();
     * try {
     *     $formattedName = $metricsScopesClient->monitoredProjectName('[METRICS_SCOPE]', '[PROJECT]');
     *     $operationResponse = $metricsScopesClient->deleteMonitoredProject($formattedName);
     *     $operationResponse->pollUntilComplete();
     *     if ($operationResponse->operationSucceeded()) {
     *         // operation succeeded and returns no value
     *     } else {
     *         $error = $operationResponse->getError();
     *         // handleError($error)
     *     }
     *     // Alternatively:
     *     // start the operation, keep the operation name, and resume later
     *     $operationResponse = $metricsScopesClient->deleteMonitoredProject($formattedName);
     *     $operationName = $operationResponse->getName();
     *     // ... do other work
     *     $newOperationResponse = $metricsScopesClient->resumeOperation($operationName, 'deleteMonitoredProject');
     *     while (!$newOperationResponse->isDone()) {
     *         // ... do other work
     *         $newOperationResponse->reload();
     *     }
     *     if ($newOperationResponse->operationSucceeded()) {
     *         // operation succeeded and returns no value
     *     } else {
     *         $error = $newOperationResponse->getError();
     *         // handleError($error)
     *     }
     * } finally {
     *     $metricsScopesClient->close();
     * }
     * ```
     *
     * @param string $name         Required. The resource name of the `MonitoredProject`.
     *                             Example:
     *                             `locations/global/metricsScopes/{SCOPING_PROJECT_ID_OR_NUMBER}/projects/{MONITORED_PROJECT_ID_OR_NUMBER}`
     *
     *                             Authorization requires the following [Google
     *                             IAM](https://cloud.google.com/iam) permissions on both the `Metrics Scope`
     *                             and on the `MonitoredProject`: `monitoring.metricsScopes.link`
     * @param array  $optionalArgs {
     *     Optional.
     *
     *     @type RetrySettings|array $retrySettings
     *           Retry settings to use for this call. Can be a
     *           {@see Google\ApiCore\RetrySettings} object, or an associative array of retry
     *           settings parameters. See the documentation on
     *           {@see Google\ApiCore\RetrySettings} for example usage.
     * }
     *
     * @return \Google\ApiCore\OperationResponse
     *
     * @throws ApiException if the remote call fails
     */
    public function deleteMonitoredProject($name, array $optionalArgs = [])
    {
        $request = new DeleteMonitoredProjectRequest();
        $requestParamHeaders = [];
        $request->setName($name);
        $requestParamHeaders['name'] = $name;
        $requestParams = new RequestParamsHeaderDescriptor($requestParamHeaders);
        $optionalArgs['headers'] = isset($optionalArgs['headers']) ? array_merge($requestParams->getHeader(), $optionalArgs['headers']) : $requestParams->getHeader();
        return $this->startOperationsCall('DeleteMonitoredProject', $optionalArgs, $request, $this->getOperationsClient())->wait();
    }

    /**
     * Returns a specific `Metrics Scope`.
     *
     * Sample code:
     * ```
     * $metricsScopesClient = new MetricsScopesClient();
     * try {
     *     $formattedName = $metricsScopesClient->metricsScopeName('[METRICS_SCOPE]');
     *     $response = $metricsScopesClient->getMetricsScope($formattedName);
     * } finally {
     *     $metricsScopesClient->close();
     * }
     * ```
     *
     * @param string $name         Required. The resource name of the `Metrics Scope`.
     *                             Example:
     *                             `locations/global/metricsScopes/{SCOPING_PROJECT_ID_OR_NUMBER}`
     * @param array  $optionalArgs {
     *     Optional.
     *
     *     @type RetrySettings|array $retrySettings
     *           Retry settings to use for this call. Can be a
     *           {@see Google\ApiCore\RetrySettings} object, or an associative array of retry
     *           settings parameters. See the documentation on
     *           {@see Google\ApiCore\RetrySettings} for example usage.
     * }
     *
     * @return \Google\Cloud\Monitoring\MetricsScope\V1\MetricsScope
     *
     * @throws ApiException if the remote call fails
     */
    public function getMetricsScope($name, array $optionalArgs = [])
    {
        $request = new GetMetricsScopeRequest();
        $requestParamHeaders = [];
        $request->setName($name);
        $requestParamHeaders['name'] = $name;
        $requestParams = new RequestParamsHeaderDescriptor($requestParamHeaders);
        $optionalArgs['headers'] = isset($optionalArgs['headers']) ? array_merge($requestParams->getHeader(), $optionalArgs['headers']) : $requestParams->getHeader();
        return $this->startCall('GetMetricsScope', MetricsScope::class, $optionalArgs, $request)->wait();
    }

    /**
     * Returns a list of every `Metrics Scope` that a specific `MonitoredProject`
     * has been added to. The metrics scope representing the specified monitored
     * project will always be the first entry in the response.
     *
     * Sample code:
     * ```
     * $metricsScopesClient = new MetricsScopesClient();
     * try {
     *     $monitoredResourceContainer = 'monitored_resource_container';
     *     $response = $metricsScopesClient->listMetricsScopesByMonitoredProject($monitoredResourceContainer);
     * } finally {
     *     $metricsScopesClient->close();
     * }
     * ```
     *
     * @param string $monitoredResourceContainer Required. The resource name of the `Monitored Project` being requested.
     *                                           Example:
     *                                           `projects/{MONITORED_PROJECT_ID_OR_NUMBER}`
     * @param array  $optionalArgs               {
     *     Optional.
     *
     *     @type RetrySettings|array $retrySettings
     *           Retry settings to use for this call. Can be a
     *           {@see Google\ApiCore\RetrySettings} object, or an associative array of retry
     *           settings parameters. See the documentation on
     *           {@see Google\ApiCore\RetrySettings} for example usage.
     * }
     *
     * @return \Google\Cloud\Monitoring\MetricsScope\V1\ListMetricsScopesByMonitoredProjectResponse
     *
     * @throws ApiException if the remote call fails
     */
    public function listMetricsScopesByMonitoredProject($monitoredResourceContainer, array $optionalArgs = [])
    {
        $request = new ListMetricsScopesByMonitoredProjectRequest();
        $request->setMonitoredResourceContainer($monitoredResourceContainer);
        return $this->startCall('ListMetricsScopesByMonitoredProject', ListMetricsScopesByMonitoredProjectResponse::class, $optionalArgs, $request)->wait();
    }
}
