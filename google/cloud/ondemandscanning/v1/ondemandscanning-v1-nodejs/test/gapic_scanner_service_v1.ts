// Copyright 2021 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// ** This file is automatically generated by gapic-generator-typescript. **
// ** https://github.com/googleapis/gapic-generator-typescript **
// ** All changes to this file may be overwritten. **

import * as protos from '../protos/protos';
import * as assert from 'assert';
import * as sinon from 'sinon';
import {SinonStub} from 'sinon';
import { describe, it } from 'mocha';
import * as scannerserviceModule from '../src';

import {PassThrough} from 'stream';

import {protobuf, LROperation, operationsProtos} from 'google-gax';

function generateSampleMessage<T extends object>(instance: T) {
    const filledObject = (instance.constructor as typeof protobuf.Message)
        .toObject(instance as protobuf.Message<T>, {defaults: true});
    return (instance.constructor as typeof protobuf.Message).fromObject(filledObject) as T;
}

function stubSimpleCall<ResponseType>(response?: ResponseType, error?: Error) {
    return error ? sinon.stub().rejects(error) : sinon.stub().resolves([response]);
}

function stubSimpleCallWithCallback<ResponseType>(response?: ResponseType, error?: Error) {
    return error ? sinon.stub().callsArgWith(2, error) : sinon.stub().callsArgWith(2, null, response);
}

function stubLongRunningCall<ResponseType>(response?: ResponseType, callError?: Error, lroError?: Error) {
    const innerStub = lroError ? sinon.stub().rejects(lroError) : sinon.stub().resolves([response]);
    const mockOperation = {
        promise: innerStub,
    };
    return callError ? sinon.stub().rejects(callError) : sinon.stub().resolves([mockOperation]);
}

function stubLongRunningCallWithCallback<ResponseType>(response?: ResponseType, callError?: Error, lroError?: Error) {
    const innerStub = lroError ? sinon.stub().rejects(lroError) : sinon.stub().resolves([response]);
    const mockOperation = {
        promise: innerStub,
    };
    return callError ? sinon.stub().callsArgWith(2, callError) : sinon.stub().callsArgWith(2, null, mockOperation);
}

function stubPageStreamingCall<ResponseType>(responses?: ResponseType[], error?: Error) {
    const pagingStub = sinon.stub();
    if (responses) {
        for (let i = 0; i < responses.length; ++i) {
            pagingStub.onCall(i).callsArgWith(2, null, responses[i]);
        }
    }
    const transformStub = error ? sinon.stub().callsArgWith(2, error) : pagingStub;
    const mockStream = new PassThrough({
        objectMode: true,
        transform: transformStub,
    });
    // trigger as many responses as needed
    if (responses) {
        for (let i = 0; i < responses.length; ++i) {
            setImmediate(() => { mockStream.write({}); });
        }
        setImmediate(() => { mockStream.end(); });
    } else {
        setImmediate(() => { mockStream.write({}); });
        setImmediate(() => { mockStream.end(); });
    }
    return sinon.stub().returns(mockStream);
}

function stubAsyncIterationCall<ResponseType>(responses?: ResponseType[], error?: Error) {
    let counter = 0;
    const asyncIterable = {
        [Symbol.asyncIterator]() {
            return {
                async next() {
                    if (error) {
                        return Promise.reject(error);
                    }
                    if (counter >= responses!.length) {
                        return Promise.resolve({done: true, value: undefined});
                    }
                    return Promise.resolve({done: false, value: responses![counter++]});
                }
            };
        }
    };
    return sinon.stub().returns(asyncIterable);
}

describe('v1.ScannerServiceClient', () => {
    it('has servicePath', () => {
        const servicePath = scannerserviceModule.v1.ScannerServiceClient.servicePath;
        assert(servicePath);
    });

    it('has apiEndpoint', () => {
        const apiEndpoint = scannerserviceModule.v1.ScannerServiceClient.apiEndpoint;
        assert(apiEndpoint);
    });

    it('has port', () => {
        const port = scannerserviceModule.v1.ScannerServiceClient.port;
        assert(port);
        assert(typeof port === 'number');
    });

    it('should create a client with no option', () => {
        const client = new scannerserviceModule.v1.ScannerServiceClient();
        assert(client);
    });

    it('should create a client with gRPC fallback', () => {
        const client = new scannerserviceModule.v1.ScannerServiceClient({
            fallback: true,
        });
        assert(client);
    });

    it('has initialize method and supports deferred initialization', async () => {
        const client = new scannerserviceModule.v1.ScannerServiceClient({
              credentials: {client_email: 'bogus', private_key: 'bogus'},
              projectId: 'bogus',
        });
        assert.strictEqual(client.scannerServiceStub, undefined);
        await client.initialize();
        assert(client.scannerServiceStub);
    });

    it('has close method', () => {
        const client = new scannerserviceModule.v1.ScannerServiceClient({
              credentials: {client_email: 'bogus', private_key: 'bogus'},
              projectId: 'bogus',
        });
        client.close();
    });

    it('has getProjectId method', async () => {
        const fakeProjectId = 'fake-project-id';
        const client = new scannerserviceModule.v1.ScannerServiceClient({
              credentials: {client_email: 'bogus', private_key: 'bogus'},
              projectId: 'bogus',
        });
        client.auth.getProjectId = sinon.stub().resolves(fakeProjectId);
        const result = await client.getProjectId();
        assert.strictEqual(result, fakeProjectId);
        assert((client.auth.getProjectId as SinonStub).calledWithExactly());
    });

    it('has getProjectId method with callback', async () => {
        const fakeProjectId = 'fake-project-id';
        const client = new scannerserviceModule.v1.ScannerServiceClient({
              credentials: {client_email: 'bogus', private_key: 'bogus'},
              projectId: 'bogus',
        });
        client.auth.getProjectId = sinon.stub().callsArgWith(0, null, fakeProjectId);
        const promise = new Promise((resolve, reject) => {
            client.getProjectId((err?: Error|null, projectId?: string|null) => {
                if (err) {
                    reject(err);
                } else {
                    resolve(projectId);
                }
            });
        });
        const result = await promise;
        assert.strictEqual(result, fakeProjectId);
    });

    describe('analyzePackages', () => {
        it('invokes analyzePackages without error', async () => {
            const client = new scannerserviceModule.v1.ScannerServiceClient({
              credentials: {client_email: 'bogus', private_key: 'bogus'},
              projectId: 'bogus',
        });
            client.initialize();
            const request = generateSampleMessage(new protos.google.cloud.ondemandscanning.v1.AnalyzePackagesRequest());
            request.parent = '';
            const expectedHeaderRequestParams = "parent=";
            const expectedOptions = {
                otherArgs: {
                    headers: {
                        'x-goog-request-params': expectedHeaderRequestParams,
                    },
                },
            };
            const expectedResponse = generateSampleMessage(new protos.google.longrunning.Operation());
            client.innerApiCalls.analyzePackages = stubLongRunningCall(expectedResponse);
            const [operation] = await client.analyzePackages(request);
            const [response] = await operation.promise();
            assert.deepStrictEqual(response, expectedResponse);
            assert((client.innerApiCalls.analyzePackages as SinonStub)
                .getCall(0).calledWith(request, expectedOptions, undefined));
        });

        it('invokes analyzePackages without error using callback', async () => {
            const client = new scannerserviceModule.v1.ScannerServiceClient({
              credentials: {client_email: 'bogus', private_key: 'bogus'},
              projectId: 'bogus',
        });
            client.initialize();
            const request = generateSampleMessage(new protos.google.cloud.ondemandscanning.v1.AnalyzePackagesRequest());
            request.parent = '';
            const expectedHeaderRequestParams = "parent=";
            const expectedOptions = {
                otherArgs: {
                    headers: {
                        'x-goog-request-params': expectedHeaderRequestParams,
                    },
                },
            };
            const expectedResponse = generateSampleMessage(new protos.google.longrunning.Operation());
            client.innerApiCalls.analyzePackages = stubLongRunningCallWithCallback(expectedResponse);
            const promise = new Promise((resolve, reject) => {
                 client.analyzePackages(
                    request,
                    (err?: Error|null,
                     result?: LROperation<protos.g.IAnalyzePackagesResponse, protos.g.IAnalyzePackagesMetadata>|null
                    ) => {
                        if (err) {
                            reject(err);
                        } else {
                            resolve(result);
                        }
                    });
            });
            const operation = await promise as LROperation<protos.g.IAnalyzePackagesResponse, protos.g.IAnalyzePackagesMetadata>;
            const [response] = await operation.promise();
            assert.deepStrictEqual(response, expectedResponse);
            assert((client.innerApiCalls.analyzePackages as SinonStub)
                .getCall(0).calledWith(request, expectedOptions /*, callback defined above */));
        });

        it('invokes analyzePackages with call error', async () => {
            const client = new scannerserviceModule.v1.ScannerServiceClient({
              credentials: {client_email: 'bogus', private_key: 'bogus'},
              projectId: 'bogus',
        });
            client.initialize();
            const request = generateSampleMessage(new protos.google.cloud.ondemandscanning.v1.AnalyzePackagesRequest());
            request.parent = '';
            const expectedHeaderRequestParams = "parent=";
            const expectedOptions = {
                otherArgs: {
                    headers: {
                        'x-goog-request-params': expectedHeaderRequestParams,
                    },
                },
            };
            const expectedError = new Error('expected');
            client.innerApiCalls.analyzePackages = stubLongRunningCall(undefined, expectedError);
            await assert.rejects(client.analyzePackages(request), expectedError);
            assert((client.innerApiCalls.analyzePackages as SinonStub)
                .getCall(0).calledWith(request, expectedOptions, undefined));
        });

        it('invokes analyzePackages with LRO error', async () => {
            const client = new scannerserviceModule.v1.ScannerServiceClient({
              credentials: {client_email: 'bogus', private_key: 'bogus'},
              projectId: 'bogus',
        });
            client.initialize();
            const request = generateSampleMessage(new protos.google.cloud.ondemandscanning.v1.AnalyzePackagesRequest());
            request.parent = '';
            const expectedHeaderRequestParams = "parent=";
            const expectedOptions = {
                otherArgs: {
                    headers: {
                        'x-goog-request-params': expectedHeaderRequestParams,
                    },
                },
            };
            const expectedError = new Error('expected');
            client.innerApiCalls.analyzePackages = stubLongRunningCall(undefined, undefined, expectedError);
            const [operation] = await client.analyzePackages(request);
            await assert.rejects(operation.promise(), expectedError);
            assert((client.innerApiCalls.analyzePackages as SinonStub)
                .getCall(0).calledWith(request, expectedOptions, undefined));
        });

        it('invokes checkAnalyzePackagesProgress without error', async () => {
            const client = new scannerserviceModule.v1.ScannerServiceClient({
              credentials: {client_email: 'bogus', private_key: 'bogus'},
              projectId: 'bogus',
        });
            client.initialize();
            const expectedResponse = generateSampleMessage(new operationsProtos.google.longrunning.Operation());
            expectedResponse.name = 'test';
            expectedResponse.response = {type_url: 'url', value: Buffer.from('')};
            expectedResponse.metadata = {type_url: 'url', value: Buffer.from('')}

            client.operationsClient.getOperation = stubSimpleCall(expectedResponse);
            const decodedOperation = await client.checkAnalyzePackagesProgress(expectedResponse.name);
            assert.deepStrictEqual(decodedOperation.name, expectedResponse.name);
            assert(decodedOperation.metadata);
            assert((client.operationsClient.getOperation as SinonStub).getCall(0));
        });

        it('invokes checkAnalyzePackagesProgress with error', async () => {
            const client = new scannerserviceModule.v1.ScannerServiceClient({
              credentials: {client_email: 'bogus', private_key: 'bogus'},
              projectId: 'bogus',
        });
            client.initialize();
            const expectedError = new Error('expected');

            client.operationsClient.getOperation = stubSimpleCall(undefined, expectedError);
            await assert.rejects(client.checkAnalyzePackagesProgress(''), expectedError);
            assert((client.operationsClient.getOperation as SinonStub)
                .getCall(0));
        });
    });

    describe('listVulnerabilities', () => {
        it('invokes listVulnerabilities without error', async () => {
            const client = new scannerserviceModule.v1.ScannerServiceClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.cloud.ondemandscanning.v1.ListVulnerabilitiesRequest());
            request.parent = '';
            const expectedHeaderRequestParams = "parent=";
            const expectedOptions = {
                otherArgs: {
                    headers: {
                        'x-goog-request-params': expectedHeaderRequestParams,
                    },
                },
            };
            const expectedResponse = [
              generateSampleMessage(new protos.grafeas.v1.Occurrence()),
              generateSampleMessage(new protos.grafeas.v1.Occurrence()),
              generateSampleMessage(new protos.grafeas.v1.Occurrence()),
            ];
            client.innerApiCalls.listVulnerabilities = stubSimpleCall(expectedResponse);
            const [response] = await client.listVulnerabilities(request);
            assert.deepStrictEqual(response, expectedResponse);
            assert((client.innerApiCalls.listVulnerabilities as SinonStub)
                .getCall(0).calledWith(request, expectedOptions, undefined));
        });

        it('invokes listVulnerabilities without error using callback', async () => {
            const client = new scannerserviceModule.v1.ScannerServiceClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.cloud.ondemandscanning.v1.ListVulnerabilitiesRequest());
            request.parent = '';
            const expectedHeaderRequestParams = "parent=";
            const expectedOptions = {
                otherArgs: {
                    headers: {
                        'x-goog-request-params': expectedHeaderRequestParams,
                    },
                },
            };
            const expectedResponse = [
              generateSampleMessage(new protos.grafeas.v1.Occurrence()),
              generateSampleMessage(new protos.grafeas.v1.Occurrence()),
              generateSampleMessage(new protos.grafeas.v1.Occurrence()),
            ];
            client.innerApiCalls.listVulnerabilities = stubSimpleCallWithCallback(expectedResponse);
            const promise = new Promise((resolve, reject) => {
                 client.listVulnerabilities(
                    request,
                    (err?: Error|null, result?: protos.grafeas.v1.IOccurrence[]|null) => {
                        if (err) {
                            reject(err);
                        } else {
                            resolve(result);
                        }
                    });
            });
            const response = await promise;
            assert.deepStrictEqual(response, expectedResponse);
            assert((client.innerApiCalls.listVulnerabilities as SinonStub)
                .getCall(0).calledWith(request, expectedOptions /*, callback defined above */));
        });

        it('invokes listVulnerabilities with error', async () => {
            const client = new scannerserviceModule.v1.ScannerServiceClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.cloud.ondemandscanning.v1.ListVulnerabilitiesRequest());
            request.parent = '';
            const expectedHeaderRequestParams = "parent=";
            const expectedOptions = {
                otherArgs: {
                    headers: {
                        'x-goog-request-params': expectedHeaderRequestParams,
                    },
                },
            };
            const expectedError = new Error('expected');
            client.innerApiCalls.listVulnerabilities = stubSimpleCall(undefined, expectedError);
            await assert.rejects(client.listVulnerabilities(request), expectedError);
            assert((client.innerApiCalls.listVulnerabilities as SinonStub)
                .getCall(0).calledWith(request, expectedOptions, undefined));
        });

        it('invokes listVulnerabilitiesStream without error', async () => {
            const client = new scannerserviceModule.v1.ScannerServiceClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.cloud.ondemandscanning.v1.ListVulnerabilitiesRequest());
            request.parent = '';
            const expectedHeaderRequestParams = "parent=";
            const expectedResponse = [
              generateSampleMessage(new protos.grafeas.v1.Occurrence()),
              generateSampleMessage(new protos.grafeas.v1.Occurrence()),
              generateSampleMessage(new protos.grafeas.v1.Occurrence()),
            ];
            client.descriptors.page.listVulnerabilities.createStream = stubPageStreamingCall(expectedResponse);
            const stream = client.listVulnerabilitiesStream(request);
            const promise = new Promise((resolve, reject) => {
                const responses: protos.grafeas.v1.Occurrence[] = [];
                stream.on('data', (response: protos.grafeas.v1.Occurrence) => {
                    responses.push(response);
                });
                stream.on('end', () => {
                    resolve(responses);
                });
                stream.on('error', (err: Error) => {
                    reject(err);
                });
            });
            const responses = await promise;
            assert.deepStrictEqual(responses, expectedResponse);
            assert((client.descriptors.page.listVulnerabilities.createStream as SinonStub)
                .getCall(0).calledWith(client.innerApiCalls.listVulnerabilities, request));
            assert.strictEqual(
                (client.descriptors.page.listVulnerabilities.createStream as SinonStub)
                    .getCall(0).args[2].otherArgs.headers['x-goog-request-params'],
                expectedHeaderRequestParams
            );
        });

        it('invokes listVulnerabilitiesStream with error', async () => {
            const client = new scannerserviceModule.v1.ScannerServiceClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.cloud.ondemandscanning.v1.ListVulnerabilitiesRequest());
            request.parent = '';
            const expectedHeaderRequestParams = "parent=";
            const expectedError = new Error('expected');
            client.descriptors.page.listVulnerabilities.createStream = stubPageStreamingCall(undefined, expectedError);
            const stream = client.listVulnerabilitiesStream(request);
            const promise = new Promise((resolve, reject) => {
                const responses: protos.grafeas.v1.Occurrence[] = [];
                stream.on('data', (response: protos.grafeas.v1.Occurrence) => {
                    responses.push(response);
                });
                stream.on('end', () => {
                    resolve(responses);
                });
                stream.on('error', (err: Error) => {
                    reject(err);
                });
            });
            await assert.rejects(promise, expectedError);
            assert((client.descriptors.page.listVulnerabilities.createStream as SinonStub)
                .getCall(0).calledWith(client.innerApiCalls.listVulnerabilities, request));
            assert.strictEqual(
                (client.descriptors.page.listVulnerabilities.createStream as SinonStub)
                    .getCall(0).args[2].otherArgs.headers['x-goog-request-params'],
                expectedHeaderRequestParams
            );
        });

        it('uses async iteration with listVulnerabilities without error', async () => {
            const client = new scannerserviceModule.v1.ScannerServiceClient({
              credentials: {client_email: 'bogus', private_key: 'bogus'},
              projectId: 'bogus',
        });
            client.initialize();
            const request = generateSampleMessage(new protos.google.cloud.ondemandscanning.v1.ListVulnerabilitiesRequest());
            request.parent = '';
            const expectedHeaderRequestParams = "parent=";
            const expectedResponse = [
              generateSampleMessage(new protos.grafeas.v1.Occurrence()),
              generateSampleMessage(new protos.grafeas.v1.Occurrence()),
              generateSampleMessage(new protos.grafeas.v1.Occurrence()),
            ];
            client.descriptors.page.listVulnerabilities.asyncIterate = stubAsyncIterationCall(expectedResponse);
            const responses: protos.grafeas.v1.IOccurrence[] = [];
            const iterable = client.listVulnerabilitiesAsync(request);
            for await (const resource of iterable) {
                responses.push(resource!);
            }
            assert.deepStrictEqual(responses, expectedResponse);
            assert.deepStrictEqual(
                (client.descriptors.page.listVulnerabilities.asyncIterate as SinonStub)
                    .getCall(0).args[1], request);
            assert.strictEqual(
                (client.descriptors.page.listVulnerabilities.asyncIterate as SinonStub)
                    .getCall(0).args[2].otherArgs.headers['x-goog-request-params'],
                expectedHeaderRequestParams
            );
        });

        it('uses async iteration with listVulnerabilities with error', async () => {
            const client = new scannerserviceModule.v1.ScannerServiceClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.cloud.ondemandscanning.v1.ListVulnerabilitiesRequest());
            request.parent = '';
            const expectedHeaderRequestParams = "parent=";const expectedError = new Error('expected');
            client.descriptors.page.listVulnerabilities.asyncIterate = stubAsyncIterationCall(undefined, expectedError);
            const iterable = client.listVulnerabilitiesAsync(request);
            await assert.rejects(async () => {
                const responses: protos.grafeas.v1.IOccurrence[] = [];
                for await (const resource of iterable) {
                    responses.push(resource!);
                }
            });
            assert.deepStrictEqual(
                (client.descriptors.page.listVulnerabilities.asyncIterate as SinonStub)
                    .getCall(0).args[1], request);
            assert.strictEqual(
                (client.descriptors.page.listVulnerabilities.asyncIterate as SinonStub)
                    .getCall(0).args[2].otherArgs.headers['x-goog-request-params'],
                expectedHeaderRequestParams
            );
        });
    });

    describe('Path templates', () => {

        describe('note', () => {
            const fakePath = "/rendered/path/note";
            const expectedParameters = {
                project: "projectValue",
                note: "noteValue",
            };
            const client = new scannerserviceModule.v1.ScannerServiceClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            client.pathTemplates.notePathTemplate.render =
                sinon.stub().returns(fakePath);
            client.pathTemplates.notePathTemplate.match =
                sinon.stub().returns(expectedParameters);

            it('notePath', () => {
                const result = client.notePath("projectValue", "noteValue");
                assert.strictEqual(result, fakePath);
                assert((client.pathTemplates.notePathTemplate.render as SinonStub)
                    .getCall(-1).calledWith(expectedParameters));
            });

            it('matchProjectFromNoteName', () => {
                const result = client.matchProjectFromNoteName(fakePath);
                assert.strictEqual(result, "projectValue");
                assert((client.pathTemplates.notePathTemplate.match as SinonStub)
                    .getCall(-1).calledWith(fakePath));
            });

            it('matchNoteFromNoteName', () => {
                const result = client.matchNoteFromNoteName(fakePath);
                assert.strictEqual(result, "noteValue");
                assert((client.pathTemplates.notePathTemplate.match as SinonStub)
                    .getCall(-1).calledWith(fakePath));
            });
        });

        describe('occurrence', () => {
            const fakePath = "/rendered/path/occurrence";
            const expectedParameters = {
                project: "projectValue",
                occurrence: "occurrenceValue",
            };
            const client = new scannerserviceModule.v1.ScannerServiceClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            client.pathTemplates.occurrencePathTemplate.render =
                sinon.stub().returns(fakePath);
            client.pathTemplates.occurrencePathTemplate.match =
                sinon.stub().returns(expectedParameters);

            it('occurrencePath', () => {
                const result = client.occurrencePath("projectValue", "occurrenceValue");
                assert.strictEqual(result, fakePath);
                assert((client.pathTemplates.occurrencePathTemplate.render as SinonStub)
                    .getCall(-1).calledWith(expectedParameters));
            });

            it('matchProjectFromOccurrenceName', () => {
                const result = client.matchProjectFromOccurrenceName(fakePath);
                assert.strictEqual(result, "projectValue");
                assert((client.pathTemplates.occurrencePathTemplate.match as SinonStub)
                    .getCall(-1).calledWith(fakePath));
            });

            it('matchOccurrenceFromOccurrenceName', () => {
                const result = client.matchOccurrenceFromOccurrenceName(fakePath);
                assert.strictEqual(result, "occurrenceValue");
                assert((client.pathTemplates.occurrencePathTemplate.match as SinonStub)
                    .getCall(-1).calledWith(fakePath));
            });
        });
    });
});
