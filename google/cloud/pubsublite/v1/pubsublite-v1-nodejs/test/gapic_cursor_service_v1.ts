// Copyright 2021 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// ** This file is automatically generated by gapic-generator-typescript. **
// ** https://github.com/googleapis/gapic-generator-typescript **
// ** All changes to this file may be overwritten. **

import * as protos from '../protos/protos';
import * as assert from 'assert';
import * as sinon from 'sinon';
import {SinonStub} from 'sinon';
import { describe, it } from 'mocha';
import * as cursorserviceModule from '../src';

import {PassThrough} from 'stream';

import {protobuf} from 'google-gax';

function generateSampleMessage<T extends object>(instance: T) {
    const filledObject = (instance.constructor as typeof protobuf.Message)
        .toObject(instance as protobuf.Message<T>, {defaults: true});
    return (instance.constructor as typeof protobuf.Message).fromObject(filledObject) as T;
}

function stubSimpleCall<ResponseType>(response?: ResponseType, error?: Error) {
    return error ? sinon.stub().rejects(error) : sinon.stub().resolves([response]);
}

function stubSimpleCallWithCallback<ResponseType>(response?: ResponseType, error?: Error) {
    return error ? sinon.stub().callsArgWith(2, error) : sinon.stub().callsArgWith(2, null, response);
}

function stubBidiStreamingCall<ResponseType>(response?: ResponseType, error?: Error) {
    const transformStub = error ? sinon.stub().callsArgWith(2, error) : sinon.stub().callsArgWith(2, null, response);
    const mockStream = new PassThrough({
        objectMode: true,
        transform: transformStub,
    });
    return sinon.stub().returns(mockStream);
}

function stubPageStreamingCall<ResponseType>(responses?: ResponseType[], error?: Error) {
    const pagingStub = sinon.stub();
    if (responses) {
        for (let i = 0; i < responses.length; ++i) {
            pagingStub.onCall(i).callsArgWith(2, null, responses[i]);
        }
    }
    const transformStub = error ? sinon.stub().callsArgWith(2, error) : pagingStub;
    const mockStream = new PassThrough({
        objectMode: true,
        transform: transformStub,
    });
    // trigger as many responses as needed
    if (responses) {
        for (let i = 0; i < responses.length; ++i) {
            setImmediate(() => { mockStream.write({}); });
        }
        setImmediate(() => { mockStream.end(); });
    } else {
        setImmediate(() => { mockStream.write({}); });
        setImmediate(() => { mockStream.end(); });
    }
    return sinon.stub().returns(mockStream);
}

function stubAsyncIterationCall<ResponseType>(responses?: ResponseType[], error?: Error) {
    let counter = 0;
    const asyncIterable = {
        [Symbol.asyncIterator]() {
            return {
                async next() {
                    if (error) {
                        return Promise.reject(error);
                    }
                    if (counter >= responses!.length) {
                        return Promise.resolve({done: true, value: undefined});
                    }
                    return Promise.resolve({done: false, value: responses![counter++]});
                }
            };
        }
    };
    return sinon.stub().returns(asyncIterable);
}

describe('v1.CursorServiceClient', () => {
    it('has servicePath', () => {
        const servicePath = cursorserviceModule.v1.CursorServiceClient.servicePath;
        assert(servicePath);
    });

    it('has apiEndpoint', () => {
        const apiEndpoint = cursorserviceModule.v1.CursorServiceClient.apiEndpoint;
        assert(apiEndpoint);
    });

    it('has port', () => {
        const port = cursorserviceModule.v1.CursorServiceClient.port;
        assert(port);
        assert(typeof port === 'number');
    });

    it('should create a client with no option', () => {
        const client = new cursorserviceModule.v1.CursorServiceClient();
        assert(client);
    });

    it('should create a client with gRPC fallback', () => {
        const client = new cursorserviceModule.v1.CursorServiceClient({
            fallback: true,
        });
        assert(client);
    });

    it('has initialize method and supports deferred initialization', async () => {
        const client = new cursorserviceModule.v1.CursorServiceClient({
            credentials: { client_email: 'bogus', private_key: 'bogus' },
            projectId: 'bogus',
        });
        assert.strictEqual(client.cursorServiceStub, undefined);
        await client.initialize();
        assert(client.cursorServiceStub);
    });

    it('has close method', () => {
        const client = new cursorserviceModule.v1.CursorServiceClient({
            credentials: { client_email: 'bogus', private_key: 'bogus' },
            projectId: 'bogus',
        });
        client.close();
    });

    it('has getProjectId method', async () => {
        const fakeProjectId = 'fake-project-id';
        const client = new cursorserviceModule.v1.CursorServiceClient({
            credentials: { client_email: 'bogus', private_key: 'bogus' },
            projectId: 'bogus',
        });
        client.auth.getProjectId = sinon.stub().resolves(fakeProjectId);
        const result = await client.getProjectId();
        assert.strictEqual(result, fakeProjectId);
        assert((client.auth.getProjectId as SinonStub).calledWithExactly());
    });

    it('has getProjectId method with callback', async () => {
        const fakeProjectId = 'fake-project-id';
        const client = new cursorserviceModule.v1.CursorServiceClient({
            credentials: { client_email: 'bogus', private_key: 'bogus' },
            projectId: 'bogus',
        });
        client.auth.getProjectId = sinon.stub().callsArgWith(0, null, fakeProjectId);
        const promise = new Promise((resolve, reject) => {
            client.getProjectId((err?: Error|null, projectId?: string|null) => {
                if (err) {
                    reject(err);
                } else {
                    resolve(projectId);
                }
            });
        });
        const result = await promise;
        assert.strictEqual(result, fakeProjectId);
    });

    describe('commitCursor', () => {
        it('invokes commitCursor without error', async () => {
            const client = new cursorserviceModule.v1.CursorServiceClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.cloud.pubsublite.v1.CommitCursorRequest());
            request.subscription = '';
            const expectedHeaderRequestParams = "subscription=";
            const expectedOptions = {
                otherArgs: {
                    headers: {
                        'x-goog-request-params': expectedHeaderRequestParams,
                    },
                },
            };
            const expectedResponse = generateSampleMessage(new protos.google.cloud.pubsublite.v1.CommitCursorResponse());
            client.innerApiCalls.commitCursor = stubSimpleCall(expectedResponse);
            const [response] = await client.commitCursor(request);
            assert.deepStrictEqual(response, expectedResponse);
            assert((client.innerApiCalls.commitCursor as SinonStub)
                .getCall(0).calledWith(request, expectedOptions, undefined));
        });

        it('invokes commitCursor without error using callback', async () => {
            const client = new cursorserviceModule.v1.CursorServiceClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.cloud.pubsublite.v1.CommitCursorRequest());
            request.subscription = '';
            const expectedHeaderRequestParams = "subscription=";
            const expectedOptions = {
                otherArgs: {
                    headers: {
                        'x-goog-request-params': expectedHeaderRequestParams,
                    },
                },
            };
            const expectedResponse = generateSampleMessage(new protos.google.cloud.pubsublite.v1.CommitCursorResponse());
            client.innerApiCalls.commitCursor = stubSimpleCallWithCallback(expectedResponse);
            const promise = new Promise((resolve, reject) => {
                 client.commitCursor(
                    request,
                    (err?: Error|null, result?: protos.google.cloud.pubsublite.v1.ICommitCursorResponse|null) => {
                        if (err) {
                            reject(err);
                        } else {
                            resolve(result);
                        }
                    });
            });
            const response = await promise;
            assert.deepStrictEqual(response, expectedResponse);
            assert((client.innerApiCalls.commitCursor as SinonStub)
                .getCall(0).calledWith(request, expectedOptions /*, callback defined above */));
        });

        it('invokes commitCursor with error', async () => {
            const client = new cursorserviceModule.v1.CursorServiceClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.cloud.pubsublite.v1.CommitCursorRequest());
            request.subscription = '';
            const expectedHeaderRequestParams = "subscription=";
            const expectedOptions = {
                otherArgs: {
                    headers: {
                        'x-goog-request-params': expectedHeaderRequestParams,
                    },
                },
            };
            const expectedError = new Error('expected');
            client.innerApiCalls.commitCursor = stubSimpleCall(undefined, expectedError);
            await assert.rejects(client.commitCursor(request), expectedError);
            assert((client.innerApiCalls.commitCursor as SinonStub)
                .getCall(0).calledWith(request, expectedOptions, undefined));
        });
    });

    describe('streamingCommitCursor', () => {
        it('invokes streamingCommitCursor without error', async () => {
            const client = new cursorserviceModule.v1.CursorServiceClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.cloud.pubsublite.v1.StreamingCommitCursorRequest());
            const expectedResponse = generateSampleMessage(new protos.google.cloud.pubsublite.v1.StreamingCommitCursorResponse());
            client.innerApiCalls.streamingCommitCursor = stubBidiStreamingCall(expectedResponse);
            const stream = client.streamingCommitCursor();
            const promise = new Promise((resolve, reject) => {
                stream.on('data', (response: protos.google.cloud.pubsublite.v1.StreamingCommitCursorResponse) => {
                    resolve(response);
                });
                stream.on('error', (err: Error) => {
                    reject(err);
                });
                stream.write(request);
                stream.end();
            });
            const response = await promise;
            assert.deepStrictEqual(response, expectedResponse);
            assert((client.innerApiCalls.streamingCommitCursor as SinonStub)
                .getCall(0).calledWithExactly(undefined));
            assert.deepStrictEqual(((stream as unknown as PassThrough)
                ._transform as SinonStub).getCall(0).args[0], request);
        });

        it('invokes streamingCommitCursor with error', async () => {
            const client = new cursorserviceModule.v1.CursorServiceClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.cloud.pubsublite.v1.StreamingCommitCursorRequest());const expectedError = new Error('expected');
            client.innerApiCalls.streamingCommitCursor = stubBidiStreamingCall(undefined, expectedError);
            const stream = client.streamingCommitCursor();
            const promise = new Promise((resolve, reject) => {
                stream.on('data', (response: protos.google.cloud.pubsublite.v1.StreamingCommitCursorResponse) => {
                    resolve(response);
                });
                stream.on('error', (err: Error) => {
                    reject(err);
                });
                stream.write(request);
                stream.end();
            });
            await assert.rejects(promise, expectedError);
            assert((client.innerApiCalls.streamingCommitCursor as SinonStub)
                .getCall(0).calledWithExactly(undefined));
            assert.deepStrictEqual(((stream as unknown as PassThrough)
                ._transform as SinonStub).getCall(0).args[0], request);
        });
    });

    describe('listPartitionCursors', () => {
        it('invokes listPartitionCursors without error', async () => {
            const client = new cursorserviceModule.v1.CursorServiceClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.cloud.pubsublite.v1.ListPartitionCursorsRequest());
            request.parent = '';
            const expectedHeaderRequestParams = "parent=";
            const expectedOptions = {
                otherArgs: {
                    headers: {
                        'x-goog-request-params': expectedHeaderRequestParams,
                    },
                },
            };
            const expectedResponse = [
              generateSampleMessage(new protos.google.cloud.pubsublite.v1.PartitionCursor()),
              generateSampleMessage(new protos.google.cloud.pubsublite.v1.PartitionCursor()),
              generateSampleMessage(new protos.google.cloud.pubsublite.v1.PartitionCursor()),
            ];
            client.innerApiCalls.listPartitionCursors = stubSimpleCall(expectedResponse);
            const [response] = await client.listPartitionCursors(request);
            assert.deepStrictEqual(response, expectedResponse);
            assert((client.innerApiCalls.listPartitionCursors as SinonStub)
                .getCall(0).calledWith(request, expectedOptions, undefined));
        });

        it('invokes listPartitionCursors without error using callback', async () => {
            const client = new cursorserviceModule.v1.CursorServiceClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.cloud.pubsublite.v1.ListPartitionCursorsRequest());
            request.parent = '';
            const expectedHeaderRequestParams = "parent=";
            const expectedOptions = {
                otherArgs: {
                    headers: {
                        'x-goog-request-params': expectedHeaderRequestParams,
                    },
                },
            };
            const expectedResponse = [
              generateSampleMessage(new protos.google.cloud.pubsublite.v1.PartitionCursor()),
              generateSampleMessage(new protos.google.cloud.pubsublite.v1.PartitionCursor()),
              generateSampleMessage(new protos.google.cloud.pubsublite.v1.PartitionCursor()),
            ];
            client.innerApiCalls.listPartitionCursors = stubSimpleCallWithCallback(expectedResponse);
            const promise = new Promise((resolve, reject) => {
                 client.listPartitionCursors(
                    request,
                    (err?: Error|null, result?: protos.google.cloud.pubsublite.v1.IPartitionCursor[]|null) => {
                        if (err) {
                            reject(err);
                        } else {
                            resolve(result);
                        }
                    });
            });
            const response = await promise;
            assert.deepStrictEqual(response, expectedResponse);
            assert((client.innerApiCalls.listPartitionCursors as SinonStub)
                .getCall(0).calledWith(request, expectedOptions /*, callback defined above */));
        });

        it('invokes listPartitionCursors with error', async () => {
            const client = new cursorserviceModule.v1.CursorServiceClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.cloud.pubsublite.v1.ListPartitionCursorsRequest());
            request.parent = '';
            const expectedHeaderRequestParams = "parent=";
            const expectedOptions = {
                otherArgs: {
                    headers: {
                        'x-goog-request-params': expectedHeaderRequestParams,
                    },
                },
            };
            const expectedError = new Error('expected');
            client.innerApiCalls.listPartitionCursors = stubSimpleCall(undefined, expectedError);
            await assert.rejects(client.listPartitionCursors(request), expectedError);
            assert((client.innerApiCalls.listPartitionCursors as SinonStub)
                .getCall(0).calledWith(request, expectedOptions, undefined));
        });

        it('invokes listPartitionCursorsStream without error', async () => {
            const client = new cursorserviceModule.v1.CursorServiceClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.cloud.pubsublite.v1.ListPartitionCursorsRequest());
            request.parent = '';
            const expectedHeaderRequestParams = "parent=";
            const expectedResponse = [
              generateSampleMessage(new protos.google.cloud.pubsublite.v1.PartitionCursor()),
              generateSampleMessage(new protos.google.cloud.pubsublite.v1.PartitionCursor()),
              generateSampleMessage(new protos.google.cloud.pubsublite.v1.PartitionCursor()),
            ];
            client.descriptors.page.listPartitionCursors.createStream = stubPageStreamingCall(expectedResponse);
            const stream = client.listPartitionCursorsStream(request);
            const promise = new Promise((resolve, reject) => {
                const responses: protos.google.cloud.pubsublite.v1.PartitionCursor[] = [];
                stream.on('data', (response: protos.google.cloud.pubsublite.v1.PartitionCursor) => {
                    responses.push(response);
                });
                stream.on('end', () => {
                    resolve(responses);
                });
                stream.on('error', (err: Error) => {
                    reject(err);
                });
            });
            const responses = await promise;
            assert.deepStrictEqual(responses, expectedResponse);
            assert((client.descriptors.page.listPartitionCursors.createStream as SinonStub)
                .getCall(0).calledWith(client.innerApiCalls.listPartitionCursors, request));
            assert.strictEqual(
                (client.descriptors.page.listPartitionCursors.createStream as SinonStub)
                    .getCall(0).args[2].otherArgs.headers['x-goog-request-params'],
                expectedHeaderRequestParams
            );
        });

        it('invokes listPartitionCursorsStream with error', async () => {
            const client = new cursorserviceModule.v1.CursorServiceClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.cloud.pubsublite.v1.ListPartitionCursorsRequest());
            request.parent = '';
            const expectedHeaderRequestParams = "parent=";
            const expectedError = new Error('expected');
            client.descriptors.page.listPartitionCursors.createStream = stubPageStreamingCall(undefined, expectedError);
            const stream = client.listPartitionCursorsStream(request);
            const promise = new Promise((resolve, reject) => {
                const responses: protos.google.cloud.pubsublite.v1.PartitionCursor[] = [];
                stream.on('data', (response: protos.google.cloud.pubsublite.v1.PartitionCursor) => {
                    responses.push(response);
                });
                stream.on('end', () => {
                    resolve(responses);
                });
                stream.on('error', (err: Error) => {
                    reject(err);
                });
            });
            await assert.rejects(promise, expectedError);
            assert((client.descriptors.page.listPartitionCursors.createStream as SinonStub)
                .getCall(0).calledWith(client.innerApiCalls.listPartitionCursors, request));
            assert.strictEqual(
                (client.descriptors.page.listPartitionCursors.createStream as SinonStub)
                    .getCall(0).args[2].otherArgs.headers['x-goog-request-params'],
                expectedHeaderRequestParams
            );
        });

        it('uses async iteration with listPartitionCursors without error', async () => {
            const client = new cursorserviceModule.v1.CursorServiceClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.cloud.pubsublite.v1.ListPartitionCursorsRequest());
            request.parent = '';
            const expectedHeaderRequestParams = "parent=";const expectedResponse = [
              generateSampleMessage(new protos.google.cloud.pubsublite.v1.PartitionCursor()),
              generateSampleMessage(new protos.google.cloud.pubsublite.v1.PartitionCursor()),
              generateSampleMessage(new protos.google.cloud.pubsublite.v1.PartitionCursor()),
            ];
            client.descriptors.page.listPartitionCursors.asyncIterate = stubAsyncIterationCall(expectedResponse);
            const responses: protos.google.cloud.pubsublite.v1.IPartitionCursor[] = [];
            const iterable = client.listPartitionCursorsAsync(request);
            for await (const resource of iterable) {
                responses.push(resource!);
            }
            assert.deepStrictEqual(responses, expectedResponse);
            assert.deepStrictEqual(
                (client.descriptors.page.listPartitionCursors.asyncIterate as SinonStub)
                    .getCall(0).args[1], request);
            assert.strictEqual(
                (client.descriptors.page.listPartitionCursors.asyncIterate as SinonStub)
                    .getCall(0).args[2].otherArgs.headers['x-goog-request-params'],
                expectedHeaderRequestParams
            );
        });

        it('uses async iteration with listPartitionCursors with error', async () => {
            const client = new cursorserviceModule.v1.CursorServiceClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.cloud.pubsublite.v1.ListPartitionCursorsRequest());
            request.parent = '';
            const expectedHeaderRequestParams = "parent=";const expectedError = new Error('expected');
            client.descriptors.page.listPartitionCursors.asyncIterate = stubAsyncIterationCall(undefined, expectedError);
            const iterable = client.listPartitionCursorsAsync(request);
            await assert.rejects(async () => {
                const responses: protos.google.cloud.pubsublite.v1.IPartitionCursor[] = [];
                for await (const resource of iterable) {
                    responses.push(resource!);
                }
            });
            assert.deepStrictEqual(
                (client.descriptors.page.listPartitionCursors.asyncIterate as SinonStub)
                    .getCall(0).args[1], request);
            assert.strictEqual(
                (client.descriptors.page.listPartitionCursors.asyncIterate as SinonStub)
                    .getCall(0).args[2].otherArgs.headers['x-goog-request-params'],
                expectedHeaderRequestParams
            );
        });
    });

    describe('Path templates', () => {

        describe('subscription', () => {
            const fakePath = "/rendered/path/subscription";
            const expectedParameters = {
                project: "projectValue",
                location: "locationValue",
                subscription: "subscriptionValue",
            };
            const client = new cursorserviceModule.v1.CursorServiceClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            client.pathTemplates.subscriptionPathTemplate.render =
                sinon.stub().returns(fakePath);
            client.pathTemplates.subscriptionPathTemplate.match =
                sinon.stub().returns(expectedParameters);

            it('subscriptionPath', () => {
                const result = client.subscriptionPath("projectValue", "locationValue", "subscriptionValue");
                assert.strictEqual(result, fakePath);
                assert((client.pathTemplates.subscriptionPathTemplate.render as SinonStub)
                    .getCall(-1).calledWith(expectedParameters));
            });

            it('matchProjectFromSubscriptionName', () => {
                const result = client.matchProjectFromSubscriptionName(fakePath);
                assert.strictEqual(result, "projectValue");
                assert((client.pathTemplates.subscriptionPathTemplate.match as SinonStub)
                    .getCall(-1).calledWith(fakePath));
            });

            it('matchLocationFromSubscriptionName', () => {
                const result = client.matchLocationFromSubscriptionName(fakePath);
                assert.strictEqual(result, "locationValue");
                assert((client.pathTemplates.subscriptionPathTemplate.match as SinonStub)
                    .getCall(-1).calledWith(fakePath));
            });

            it('matchSubscriptionFromSubscriptionName', () => {
                const result = client.matchSubscriptionFromSubscriptionName(fakePath);
                assert.strictEqual(result, "subscriptionValue");
                assert((client.pathTemplates.subscriptionPathTemplate.match as SinonStub)
                    .getCall(-1).calledWith(fakePath));
            });
        });

        describe('topic', () => {
            const fakePath = "/rendered/path/topic";
            const expectedParameters = {
                project: "projectValue",
                location: "locationValue",
                topic: "topicValue",
            };
            const client = new cursorserviceModule.v1.CursorServiceClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            client.pathTemplates.topicPathTemplate.render =
                sinon.stub().returns(fakePath);
            client.pathTemplates.topicPathTemplate.match =
                sinon.stub().returns(expectedParameters);

            it('topicPath', () => {
                const result = client.topicPath("projectValue", "locationValue", "topicValue");
                assert.strictEqual(result, fakePath);
                assert((client.pathTemplates.topicPathTemplate.render as SinonStub)
                    .getCall(-1).calledWith(expectedParameters));
            });

            it('matchProjectFromTopicName', () => {
                const result = client.matchProjectFromTopicName(fakePath);
                assert.strictEqual(result, "projectValue");
                assert((client.pathTemplates.topicPathTemplate.match as SinonStub)
                    .getCall(-1).calledWith(fakePath));
            });

            it('matchLocationFromTopicName', () => {
                const result = client.matchLocationFromTopicName(fakePath);
                assert.strictEqual(result, "locationValue");
                assert((client.pathTemplates.topicPathTemplate.match as SinonStub)
                    .getCall(-1).calledWith(fakePath));
            });

            it('matchTopicFromTopicName', () => {
                const result = client.matchTopicFromTopicName(fakePath);
                assert.strictEqual(result, "topicValue");
                assert((client.pathTemplates.topicPathTemplate.match as SinonStub)
                    .getCall(-1).calledWith(fakePath));
            });
        });
    });
});
