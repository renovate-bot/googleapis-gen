// Copyright 2021 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// ** This file is automatically generated by gapic-generator-typescript. **
// ** https://github.com/googleapis/gapic-generator-typescript **
// ** All changes to this file may be overwritten. **

/* global window */
import * as gax from 'google-gax';
import {Callback, CallOptions, Descriptors, ClientOptions} from 'google-gax';

import * as protos from '../../protos/protos';
import jsonProtos = require('../../protos/protos.json');
/**
 * Client JSON configuration object, loaded from
 * `src/v1/home_graph_api_service_client_config.json`.
 * This file defines retry strategy and timeouts for all API methods in this library.
 */
import * as gapicConfig from './home_graph_api_service_client_config.json';

const version = require('../../../package.json').version;

/**
 *  Google Home Graph API service. The Home Graph service provides support for
 *  accessing first-party and third-party devices stored in Google's Home Graph.
 *  The Home Graph database provides contextual data about the relationships
 *  between devices and the home.
 *
 *  For more details, see the [Home Graph developer
 *  guide](https://developers.google.com/assistant/smarthome/concepts/homegraph).
 * @class
 * @memberof v1
 */
export class HomeGraphApiServiceClient {
  private _terminated = false;
  private _opts: ClientOptions;
  private _providedCustomServicePath: boolean;
  private _gaxModule: typeof gax | typeof gax.fallback;
  private _gaxGrpc: gax.GrpcClient | gax.fallback.GrpcClient;
  private _protos: {};
  private _defaults: {[method: string]: gax.CallSettings};
  auth: gax.GoogleAuth;
  descriptors: Descriptors = {
    page: {},
    stream: {},
    longrunning: {},
    batching: {},
  };
  warn: (code: string, message: string, warnType?: string) => void;
  innerApiCalls: {[name: string]: Function};
  pathTemplates: {[name: string]: gax.PathTemplate};
  homeGraphApiServiceStub?: Promise<{[name: string]: Function}>;

  /**
   * Construct an instance of HomeGraphApiServiceClient.
   *
   * @param {object} [options] - The configuration object.
   * The options accepted by the constructor are described in detail
   * in [this document](https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#creating-the-client-instance).
   * The common options are:
   * @param {object} [options.credentials] - Credentials object.
   * @param {string} [options.credentials.client_email]
   * @param {string} [options.credentials.private_key]
   * @param {string} [options.email] - Account email address. Required when
   *     using a .pem or .p12 keyFilename.
   * @param {string} [options.keyFilename] - Full path to the a .json, .pem, or
   *     .p12 key downloaded from the Google Developers Console. If you provide
   *     a path to a JSON file, the projectId option below is not necessary.
   *     NOTE: .pem and .p12 require you to specify options.email as well.
   * @param {number} [options.port] - The port on which to connect to
   *     the remote host.
   * @param {string} [options.projectId] - The project ID from the Google
   *     Developer's Console, e.g. 'grape-spaceship-123'. We will also check
   *     the environment variable GCLOUD_PROJECT for your project ID. If your
   *     app is running in an environment which supports
   *     {@link https://developers.google.com/identity/protocols/application-default-credentials Application Default Credentials},
   *     your project ID will be detected automatically.
   * @param {string} [options.apiEndpoint] - The domain name of the
   *     API remote host.
   * @param {gax.ClientConfig} [options.clientConfig] - Client configuration override.
   *     Follows the structure of {@link gapicConfig}.
   * @param {boolean} [options.fallback] - Use HTTP fallback mode.
   *     In fallback mode, a special browser-compatible transport implementation is used
   *     instead of gRPC transport. In browser context (if the `window` object is defined)
   *     the fallback mode is enabled automatically; set `options.fallback` to `false`
   *     if you need to override this behavior.
   */
  constructor(opts?: ClientOptions) {
    // Ensure that options include all the required fields.
    const staticMembers = this.constructor as typeof HomeGraphApiServiceClient;
    const servicePath = opts?.servicePath || opts?.apiEndpoint || staticMembers.servicePath;
    this._providedCustomServicePath = !!(opts?.servicePath || opts?.apiEndpoint);
    const port = opts?.port || staticMembers.port;
    const clientConfig = opts?.clientConfig ?? {};
    const fallback = opts?.fallback ?? (typeof window !== 'undefined' && typeof window?.fetch === 'function');
    opts = Object.assign({servicePath, port, clientConfig, fallback}, opts);

    // If scopes are unset in options and we're connecting to a non-default endpoint, set scopes just in case.
    if (servicePath !== staticMembers.servicePath && !('scopes' in opts)) {
      opts['scopes'] = staticMembers.scopes;
    }

    // Choose either gRPC or proto-over-HTTP implementation of google-gax.
    this._gaxModule = opts.fallback ? gax.fallback : gax;

    // Create a `gaxGrpc` object, with any grpc-specific options sent to the client.
    this._gaxGrpc = new this._gaxModule.GrpcClient(opts);

    // Save options to use in initialize() method.
    this._opts = opts;

    // Save the auth object to the client, for use by other methods.
    this.auth = (this._gaxGrpc.auth as gax.GoogleAuth);

    // Set useJWTAccessWithScope on the auth object.
    this.auth.useJWTAccessWithScope = true;

    // Set defaultServicePath on the auth object.
    this.auth.defaultServicePath = staticMembers.servicePath;

    // Set the default scopes in auth client if needed.
    if (servicePath === staticMembers.servicePath) {
      this.auth.defaultScopes = staticMembers.scopes;
    }

    // Determine the client header string.
    const clientHeader = [
      `gax/${this._gaxModule.version}`,
      `gapic/${version}`,
    ];
    if (typeof process !== 'undefined' && 'versions' in process) {
      clientHeader.push(`gl-node/${process.versions.node}`);
    } else {
      clientHeader.push(`gl-web/${this._gaxModule.version}`);
    }
    if (!opts.fallback) {
      clientHeader.push(`grpc/${this._gaxGrpc.grpcVersion}`);
    } else if (opts.fallback === 'rest' ) {
      clientHeader.push(`rest/${this._gaxGrpc.grpcVersion}`);
    }
    if (opts.libName && opts.libVersion) {
      clientHeader.push(`${opts.libName}/${opts.libVersion}`);
    }
    // Load the applicable protos.
    this._protos = this._gaxGrpc.loadProtoJSON(jsonProtos);

    // This API contains "path templates"; forward-slash-separated
    // identifiers to uniquely identify resources within the API.
    // Create useful helper objects for these.
    this.pathTemplates = {
      agentUserPathPathTemplate: new this._gaxModule.PathTemplate(
        'agentUsers/{agent_user_path=**}'
      ),
    };

    // Put together the default options sent with requests.
    this._defaults = this._gaxGrpc.constructSettings(
        'google.home.graph.v1.HomeGraphApiService', gapicConfig as gax.ClientConfig,
        opts.clientConfig || {}, {'x-goog-api-client': clientHeader.join(' ')});

    // Set up a dictionary of "inner API calls"; the core implementation
    // of calling the API is handled in `google-gax`, with this code
    // merely providing the destination and request information.
    this.innerApiCalls = {};

    // Add a warn function to the client constructor so it can be easily tested.
    this.warn = gax.warn;
  }

  /**
   * Initialize the client.
   * Performs asynchronous operations (such as authentication) and prepares the client.
   * This function will be called automatically when any class method is called for the
   * first time, but if you need to initialize it before calling an actual method,
   * feel free to call initialize() directly.
   *
   * You can await on this method if you want to make sure the client is initialized.
   *
   * @returns {Promise} A promise that resolves to an authenticated service stub.
   */
  initialize() {
    // If the client stub promise is already initialized, return immediately.
    if (this.homeGraphApiServiceStub) {
      return this.homeGraphApiServiceStub;
    }

    // Put together the "service stub" for
    // google.home.graph.v1.HomeGraphApiService.
    this.homeGraphApiServiceStub = this._gaxGrpc.createStub(
        this._opts.fallback ?
          (this._protos as protobuf.Root).lookupService('google.home.graph.v1.HomeGraphApiService') :
          // eslint-disable-next-line @typescript-eslint/no-explicit-any
          (this._protos as any).google.home.graph.v1.HomeGraphApiService,
        this._opts, this._providedCustomServicePath) as Promise<{[method: string]: Function}>;

    // Iterate over each of the methods that the service provides
    // and create an API call method for each.
    const homeGraphApiServiceStubMethods =
        ['requestSyncDevices', 'reportStateAndNotification', 'deleteAgentUser', 'query', 'sync'];
    for (const methodName of homeGraphApiServiceStubMethods) {
      const callPromise = this.homeGraphApiServiceStub.then(
        stub => (...args: Array<{}>) => {
          if (this._terminated) {
            return Promise.reject('The client has already been closed.');
          }
          const func = stub[methodName];
          return func.apply(stub, args);
        },
        (err: Error|null|undefined) => () => {
          throw err;
        });

      const descriptor =
        undefined;
      const apiCall = this._gaxModule.createApiCall(
        callPromise,
        this._defaults[methodName],
        descriptor
      );

      this.innerApiCalls[methodName] = apiCall;
    }

    return this.homeGraphApiServiceStub;
  }

  /**
   * The DNS address for this API service.
   * @returns {string} The DNS address for this service.
   */
  static get servicePath() {
    return 'homegraph.googleapis.com';
  }

  /**
   * The DNS address for this API service - same as servicePath(),
   * exists for compatibility reasons.
   * @returns {string} The DNS address for this service.
   */
  static get apiEndpoint() {
    return 'homegraph.googleapis.com';
  }

  /**
   * The port for this API service.
   * @returns {number} The default port for this service.
   */
  static get port() {
    return 443;
  }

  /**
   * The scopes needed to make gRPC calls for every method defined
   * in this service.
   * @returns {string[]} List of default scopes.
   */
  static get scopes() {
    return [];
  }

  getProjectId(): Promise<string>;
  getProjectId(callback: Callback<string, undefined, undefined>): void;
  /**
   * Return the project ID used by this class.
   * @returns {Promise} A promise that resolves to string containing the project ID.
   */
  getProjectId(callback?: Callback<string, undefined, undefined>):
      Promise<string>|void {
    if (callback) {
      this.auth.getProjectId(callback);
      return;
    }
    return this.auth.getProjectId();
  }

  // -------------------
  // -- Service calls --
  // -------------------
  requestSyncDevices(
      request?: protos.google.home.graph.v1.IRequestSyncDevicesRequest,
      options?: CallOptions):
      Promise<[
        protos.google.home.graph.v1.IRequestSyncDevicesResponse,
        protos.google.home.graph.v1.IRequestSyncDevicesRequest|undefined, {}|undefined
      ]>;
  requestSyncDevices(
      request: protos.google.home.graph.v1.IRequestSyncDevicesRequest,
      options: CallOptions,
      callback: Callback<
          protos.google.home.graph.v1.IRequestSyncDevicesResponse,
          protos.google.home.graph.v1.IRequestSyncDevicesRequest|null|undefined,
          {}|null|undefined>): void;
  requestSyncDevices(
      request: protos.google.home.graph.v1.IRequestSyncDevicesRequest,
      callback: Callback<
          protos.google.home.graph.v1.IRequestSyncDevicesResponse,
          protos.google.home.graph.v1.IRequestSyncDevicesRequest|null|undefined,
          {}|null|undefined>): void;
/**
 * Requests Google to send an `action.devices.SYNC`
 * [intent](https://developers.google.com/assistant/smarthome/reference/intent/sync)
 * to your smart home Action to update device metadata for the given user.
 *
 *
 * The third-party user's identity is passed via the `agent_user_id`
 * (see
 * {@link google.home.graph.v1.RequestSyncDevicesRequest|RequestSyncDevicesRequest}).
 * This request must be authorized using service account credentials from your
 * Actions console project.
 *
 * @param {Object} request
 *   The request object that will be sent.
 * @param {string} request.agentUserId
 *   Required. Third-party user ID.
 * @param {boolean} [request.async]
 *   Optional. If set, the request will be added to a queue and a response will
 *   be returned immediately. This enables concurrent requests for the given
 *   `agent_user_id`, but the caller will not receive any error responses.
 * @param {object} [options]
 *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
 * @returns {Promise} - The promise which resolves to an array.
 *   The first element of the array is an object representing [RequestSyncDevicesResponse]{@link google.home.graph.v1.RequestSyncDevicesResponse}.
 *   Please see the
 *   [documentation](https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods)
 *   for more details and examples.
 * @example
 * const [response] = await client.requestSyncDevices(request);
 *
 * @example <caption>include:samples/generated/v1/home_graph_api_service.request_sync_devices.js</caption>
 * region_tag:graph_request_sync_devices_sample
 *
 */
  requestSyncDevices(
      request?: protos.google.home.graph.v1.IRequestSyncDevicesRequest,
      optionsOrCallback?: CallOptions|Callback<
          protos.google.home.graph.v1.IRequestSyncDevicesResponse,
          protos.google.home.graph.v1.IRequestSyncDevicesRequest|null|undefined,
          {}|null|undefined>,
      callback?: Callback<
          protos.google.home.graph.v1.IRequestSyncDevicesResponse,
          protos.google.home.graph.v1.IRequestSyncDevicesRequest|null|undefined,
          {}|null|undefined>):
      Promise<[
        protos.google.home.graph.v1.IRequestSyncDevicesResponse,
        protos.google.home.graph.v1.IRequestSyncDevicesRequest|undefined, {}|undefined
      ]>|void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    }
    else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    this.initialize();
    return this.innerApiCalls.requestSyncDevices(request, options, callback);
  }
  reportStateAndNotification(
      request?: protos.google.home.graph.v1.IReportStateAndNotificationRequest,
      options?: CallOptions):
      Promise<[
        protos.google.home.graph.v1.IReportStateAndNotificationResponse,
        protos.google.home.graph.v1.IReportStateAndNotificationRequest|undefined, {}|undefined
      ]>;
  reportStateAndNotification(
      request: protos.google.home.graph.v1.IReportStateAndNotificationRequest,
      options: CallOptions,
      callback: Callback<
          protos.google.home.graph.v1.IReportStateAndNotificationResponse,
          protos.google.home.graph.v1.IReportStateAndNotificationRequest|null|undefined,
          {}|null|undefined>): void;
  reportStateAndNotification(
      request: protos.google.home.graph.v1.IReportStateAndNotificationRequest,
      callback: Callback<
          protos.google.home.graph.v1.IReportStateAndNotificationResponse,
          protos.google.home.graph.v1.IReportStateAndNotificationRequest|null|undefined,
          {}|null|undefined>): void;
/**
 * Reports device state and optionally sends device notifications.
 * Called by your smart home Action when the state of a third-party device
 * changes or you need to send a notification about the device.
 * See [Implement Report
 * State](https://developers.google.com/assistant/smarthome/develop/report-state)
 * for more information.
 *
 * This method updates the device state according to its declared
 * [traits](https://developers.google.com/assistant/smarthome/concepts/devices-traits).
 * Publishing a new state value outside of these traits will result in an
 * `INVALID_ARGUMENT` error response.
 *
 * The third-party user's identity is passed in via the `agent_user_id`
 * (see
 * {@link google.home.graph.v1.ReportStateAndNotificationRequest|ReportStateAndNotificationRequest}).
 * This request must be authorized using service account credentials from your
 * Actions console project.
 *
 * @param {Object} request
 *   The request object that will be sent.
 * @param {string} request.requestId
 *   Request ID used for debugging.
 * @param {string} request.eventId
 *   Unique identifier per event (for example, a doorbell press).
 * @param {string} request.agentUserId
 *   Required. Third-party user ID.
 * @param {string} request.followUpToken
 *   Deprecated.
 *   (-- Token to maintain state in the follow up notification response. See the
 *   notifications guide at
 *   https://developers.google.com/assistant/smarthome/develop/notifications for
 *   details on implementing follow up notifications --)
 * @param {google.home.graph.v1.StateAndNotificationPayload} request.payload
 *   Required. State of devices to update and notification metadata for devices.
 * @param {object} [options]
 *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
 * @returns {Promise} - The promise which resolves to an array.
 *   The first element of the array is an object representing [ReportStateAndNotificationResponse]{@link google.home.graph.v1.ReportStateAndNotificationResponse}.
 *   Please see the
 *   [documentation](https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods)
 *   for more details and examples.
 * @example
 * const [response] = await client.reportStateAndNotification(request);
 *
 * @example <caption>include:samples/generated/v1/home_graph_api_service.report_state_and_notification.js</caption>
 * region_tag:graph_report_state_and_notification_sample
 *
 */
  reportStateAndNotification(
      request?: protos.google.home.graph.v1.IReportStateAndNotificationRequest,
      optionsOrCallback?: CallOptions|Callback<
          protos.google.home.graph.v1.IReportStateAndNotificationResponse,
          protos.google.home.graph.v1.IReportStateAndNotificationRequest|null|undefined,
          {}|null|undefined>,
      callback?: Callback<
          protos.google.home.graph.v1.IReportStateAndNotificationResponse,
          protos.google.home.graph.v1.IReportStateAndNotificationRequest|null|undefined,
          {}|null|undefined>):
      Promise<[
        protos.google.home.graph.v1.IReportStateAndNotificationResponse,
        protos.google.home.graph.v1.IReportStateAndNotificationRequest|undefined, {}|undefined
      ]>|void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    }
    else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    this.initialize();
    return this.innerApiCalls.reportStateAndNotification(request, options, callback);
  }
  deleteAgentUser(
      request?: protos.google.home.graph.v1.IDeleteAgentUserRequest,
      options?: CallOptions):
      Promise<[
        protos.google.protobuf.IEmpty,
        protos.google.home.graph.v1.IDeleteAgentUserRequest|undefined, {}|undefined
      ]>;
  deleteAgentUser(
      request: protos.google.home.graph.v1.IDeleteAgentUserRequest,
      options: CallOptions,
      callback: Callback<
          protos.google.protobuf.IEmpty,
          protos.google.home.graph.v1.IDeleteAgentUserRequest|null|undefined,
          {}|null|undefined>): void;
  deleteAgentUser(
      request: protos.google.home.graph.v1.IDeleteAgentUserRequest,
      callback: Callback<
          protos.google.protobuf.IEmpty,
          protos.google.home.graph.v1.IDeleteAgentUserRequest|null|undefined,
          {}|null|undefined>): void;
/**
 * Unlinks the given third-party user from your smart home Action.
 * All data related to this user will be deleted.
 *
 * For more details on how users link their accounts, see
 * [fulfillment and
 * authentication](https://developers.google.com/assistant/smarthome/concepts/fulfillment-authentication).
 *
 * The third-party user's identity is passed in via the `agent_user_id`
 * (see
 * {@link google.home.graph.v1.DeleteAgentUserRequest|DeleteAgentUserRequest}).
 * This request must be authorized using service account credentials from your
 * Actions console project.
 *
 * @param {Object} request
 *   The request object that will be sent.
 * @param {string} request.requestId
 *   Request ID used for debugging.
 * @param {string} request.agentUserId
 *   Required. Third-party user ID.
 * @param {object} [options]
 *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
 * @returns {Promise} - The promise which resolves to an array.
 *   The first element of the array is an object representing [Empty]{@link google.protobuf.Empty}.
 *   Please see the
 *   [documentation](https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods)
 *   for more details and examples.
 * @example
 * const [response] = await client.deleteAgentUser(request);
 *
 * @example <caption>include:samples/generated/v1/home_graph_api_service.delete_agent_user.js</caption>
 * region_tag:graph_delete_agent_user_sample
 *
 */
  deleteAgentUser(
      request?: protos.google.home.graph.v1.IDeleteAgentUserRequest,
      optionsOrCallback?: CallOptions|Callback<
          protos.google.protobuf.IEmpty,
          protos.google.home.graph.v1.IDeleteAgentUserRequest|null|undefined,
          {}|null|undefined>,
      callback?: Callback<
          protos.google.protobuf.IEmpty,
          protos.google.home.graph.v1.IDeleteAgentUserRequest|null|undefined,
          {}|null|undefined>):
      Promise<[
        protos.google.protobuf.IEmpty,
        protos.google.home.graph.v1.IDeleteAgentUserRequest|undefined, {}|undefined
      ]>|void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    }
    else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers[
      'x-goog-request-params'
    ] = gax.routingHeader.fromParams({
      'agent_user_id': request.agentUserId || '',
    });
    this.initialize();
    return this.innerApiCalls.deleteAgentUser(request, options, callback);
  }
  query(
      request?: protos.google.home.graph.v1.IQueryRequest,
      options?: CallOptions):
      Promise<[
        protos.google.home.graph.v1.IQueryResponse,
        protos.google.home.graph.v1.IQueryRequest|undefined, {}|undefined
      ]>;
  query(
      request: protos.google.home.graph.v1.IQueryRequest,
      options: CallOptions,
      callback: Callback<
          protos.google.home.graph.v1.IQueryResponse,
          protos.google.home.graph.v1.IQueryRequest|null|undefined,
          {}|null|undefined>): void;
  query(
      request: protos.google.home.graph.v1.IQueryRequest,
      callback: Callback<
          protos.google.home.graph.v1.IQueryResponse,
          protos.google.home.graph.v1.IQueryRequest|null|undefined,
          {}|null|undefined>): void;
/**
 * Gets the current states in Home Graph for the given set of the third-party
 * user's devices.
 *
 * The third-party user's identity is passed in via the `agent_user_id`
 * (see {@link google.home.graph.v1.QueryRequest|QueryRequest}).
 * This request must be authorized using service account credentials from your
 * Actions console project.
 *
 * @param {Object} request
 *   The request object that will be sent.
 * @param {string} request.requestId
 *   Request ID used for debugging.
 * @param {string} request.agentUserId
 *   Required. Third-party user ID.
 * @param {number[]} request.inputs
 *   Required. Inputs containing third-party device IDs for which to
 *   get the device states.
 * @param {object} [options]
 *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
 * @returns {Promise} - The promise which resolves to an array.
 *   The first element of the array is an object representing [QueryResponse]{@link google.home.graph.v1.QueryResponse}.
 *   Please see the
 *   [documentation](https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods)
 *   for more details and examples.
 * @example
 * const [response] = await client.query(request);
 *
 * @example <caption>include:samples/generated/v1/home_graph_api_service.query.js</caption>
 * region_tag:graph_query_sample
 *
 */
  query(
      request?: protos.google.home.graph.v1.IQueryRequest,
      optionsOrCallback?: CallOptions|Callback<
          protos.google.home.graph.v1.IQueryResponse,
          protos.google.home.graph.v1.IQueryRequest|null|undefined,
          {}|null|undefined>,
      callback?: Callback<
          protos.google.home.graph.v1.IQueryResponse,
          protos.google.home.graph.v1.IQueryRequest|null|undefined,
          {}|null|undefined>):
      Promise<[
        protos.google.home.graph.v1.IQueryResponse,
        protos.google.home.graph.v1.IQueryRequest|undefined, {}|undefined
      ]>|void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    }
    else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    this.initialize();
    return this.innerApiCalls.query(request, options, callback);
  }
  sync(
      request?: protos.google.home.graph.v1.ISyncRequest,
      options?: CallOptions):
      Promise<[
        protos.google.home.graph.v1.ISyncResponse,
        protos.google.home.graph.v1.ISyncRequest|undefined, {}|undefined
      ]>;
  sync(
      request: protos.google.home.graph.v1.ISyncRequest,
      options: CallOptions,
      callback: Callback<
          protos.google.home.graph.v1.ISyncResponse,
          protos.google.home.graph.v1.ISyncRequest|null|undefined,
          {}|null|undefined>): void;
  sync(
      request: protos.google.home.graph.v1.ISyncRequest,
      callback: Callback<
          protos.google.home.graph.v1.ISyncResponse,
          protos.google.home.graph.v1.ISyncRequest|null|undefined,
          {}|null|undefined>): void;
/**
 * Gets all the devices associated with the given third-party user.
 *
 * The third-party user's identity is passed in via the `agent_user_id`
 * (see {@link google.home.graph.v1.SyncRequest|SyncRequest}).
 * This request must be authorized using service account credentials from your
 * Actions console project.
 *
 * @param {Object} request
 *   The request object that will be sent.
 * @param {string} request.requestId
 *   Request ID used for debugging.
 * @param {string} request.agentUserId
 *   Required. Third-party user ID.
 * @param {object} [options]
 *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
 * @returns {Promise} - The promise which resolves to an array.
 *   The first element of the array is an object representing [SyncResponse]{@link google.home.graph.v1.SyncResponse}.
 *   Please see the
 *   [documentation](https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods)
 *   for more details and examples.
 * @example
 * const [response] = await client.sync(request);
 *
 * @example <caption>include:samples/generated/v1/home_graph_api_service.sync.js</caption>
 * region_tag:graph_sync_sample
 *
 */
  sync(
      request?: protos.google.home.graph.v1.ISyncRequest,
      optionsOrCallback?: CallOptions|Callback<
          protos.google.home.graph.v1.ISyncResponse,
          protos.google.home.graph.v1.ISyncRequest|null|undefined,
          {}|null|undefined>,
      callback?: Callback<
          protos.google.home.graph.v1.ISyncResponse,
          protos.google.home.graph.v1.ISyncRequest|null|undefined,
          {}|null|undefined>):
      Promise<[
        protos.google.home.graph.v1.ISyncResponse,
        protos.google.home.graph.v1.ISyncRequest|undefined, {}|undefined
      ]>|void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    }
    else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    this.initialize();
    return this.innerApiCalls.sync(request, options, callback);
  }

  // --------------------
  // -- Path templates --
  // --------------------

  /**
   * Return a fully-qualified agentUserPath resource name string.
   *
   * @param {string} agent_user_path
   * @returns {string} Resource name string.
   */
  agentUserPathPath(agentUserPath:string) {
    return this.pathTemplates.agentUserPathPathTemplate.render({
      agent_user_path: agentUserPath,
    });
  }

  /**
   * Parse the agent_user_path from AgentUserPath resource.
   *
   * @param {string} agentUserPathName
   *   A fully-qualified path representing AgentUserPath resource.
   * @returns {string} A string representing the agent_user_path.
   */
  matchAgentUserPathFromAgentUserPathName(agentUserPathName: string) {
    return this.pathTemplates.agentUserPathPathTemplate.match(agentUserPathName).agent_user_path;
  }

  /**
   * Terminate the gRPC channel and close the client.
   *
   * The client will no longer be usable and all future behavior is undefined.
   * @returns {Promise} A promise that resolves when the client is closed.
   */
  close(): Promise<void> {
    this.initialize();
    if (!this._terminated) {
      return this.homeGraphApiServiceStub!.then(stub => {
        this._terminated = true;
        stub.close();
      });
    }
    return Promise.resolve();
  }
}
