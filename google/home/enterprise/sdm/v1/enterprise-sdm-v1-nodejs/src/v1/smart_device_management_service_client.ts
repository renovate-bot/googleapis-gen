// Copyright 2021 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// ** This file is automatically generated by gapic-generator-typescript. **
// ** https://github.com/googleapis/gapic-generator-typescript **
// ** All changes to this file may be overwritten. **

/* global window */
import * as gax from 'google-gax';
import {Callback, CallOptions, Descriptors, ClientOptions, PaginationCallback, GaxCall} from 'google-gax';
import * as path from 'path';

import { Transform } from 'stream';
import { RequestType } from 'google-gax/build/src/apitypes';
import * as protos from '../../protos/protos';
/**
 * Client JSON configuration object, loaded from
 * `src/v1/smart_device_management_service_client_config.json`.
 * This file defines retry strategy and timeouts for all API methods in this library.
 */
import * as gapicConfig from './smart_device_management_service_client_config.json';

const version = require('../../../package.json').version;

/**
 *  A service that allows API consumers to provision and manage Google
 *  Home structures and devices for enterprise use cases.
 * @class
 * @memberof v1
 */
export class SmartDeviceManagementServiceClient {
  private _terminated = false;
  private _opts: ClientOptions;
  private _gaxModule: typeof gax | typeof gax.fallback;
  private _gaxGrpc: gax.GrpcClient | gax.fallback.GrpcClient;
  private _protos: {};
  private _defaults: {[method: string]: gax.CallSettings};
  auth: gax.GoogleAuth;
  descriptors: Descriptors = {
    page: {},
    stream: {},
    longrunning: {},
    batching: {},
  };
  innerApiCalls: {[name: string]: Function};
  pathTemplates: {[name: string]: gax.PathTemplate};
  smartDeviceManagementServiceStub?: Promise<{[name: string]: Function}>;

  /**
   * Construct an instance of SmartDeviceManagementServiceClient.
   *
   * @param {object} [options] - The configuration object.
   * The options accepted by the constructor are described in detail
   * in [this document](https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#creating-the-client-instance).
   * The common options are:
   * @param {object} [options.credentials] - Credentials object.
   * @param {string} [options.credentials.client_email]
   * @param {string} [options.credentials.private_key]
   * @param {string} [options.email] - Account email address. Required when
   *     using a .pem or .p12 keyFilename.
   * @param {string} [options.keyFilename] - Full path to the a .json, .pem, or
   *     .p12 key downloaded from the Google Developers Console. If you provide
   *     a path to a JSON file, the projectId option below is not necessary.
   *     NOTE: .pem and .p12 require you to specify options.email as well.
   * @param {number} [options.port] - The port on which to connect to
   *     the remote host.
   * @param {string} [options.projectId] - The project ID from the Google
   *     Developer's Console, e.g. 'grape-spaceship-123'. We will also check
   *     the environment variable GCLOUD_PROJECT for your project ID. If your
   *     app is running in an environment which supports
   *     {@link https://developers.google.com/identity/protocols/application-default-credentials Application Default Credentials},
   *     your project ID will be detected automatically.
   * @param {string} [options.apiEndpoint] - The domain name of the
   *     API remote host.
   * @param {gax.ClientConfig} [options.clientConfig] - Client configuration override.
   *     Follows the structure of {@link gapicConfig}.
   * @param {boolean} [options.fallback] - Use HTTP fallback mode.
   *     In fallback mode, a special browser-compatible transport implementation is used
   *     instead of gRPC transport. In browser context (if the `window` object is defined)
   *     the fallback mode is enabled automatically; set `options.fallback` to `false`
   *     if you need to override this behavior.
   */
  constructor(opts?: ClientOptions) {
    // Ensure that options include all the required fields.
    const staticMembers = this.constructor as typeof SmartDeviceManagementServiceClient;
    const servicePath = opts?.servicePath || opts?.apiEndpoint || staticMembers.servicePath;
    const port = opts?.port || staticMembers.port;
    const clientConfig = opts?.clientConfig ?? {};
    const fallback = opts?.fallback ?? (typeof window !== 'undefined' && typeof window?.fetch === 'function');
    opts = Object.assign({servicePath, port, clientConfig, fallback}, opts);

    // If scopes are unset in options and we're connecting to a non-default endpoint, set scopes just in case.
    if (servicePath !== staticMembers.servicePath && !('scopes' in opts)) {
      opts['scopes'] = staticMembers.scopes;
    }

    // Choose either gRPC or proto-over-HTTP implementation of google-gax.
    this._gaxModule = opts.fallback ? gax.fallback : gax;

    // Create a `gaxGrpc` object, with any grpc-specific options sent to the client.
    this._gaxGrpc = new this._gaxModule.GrpcClient(opts);

    // Save options to use in initialize() method.
    this._opts = opts;

    // Save the auth object to the client, for use by other methods.
    this.auth = (this._gaxGrpc.auth as gax.GoogleAuth);

    // Set the default scopes in auth client if needed.
    if (servicePath === staticMembers.servicePath) {
      this.auth.defaultScopes = staticMembers.scopes;
    }

    // Determine the client header string.
    const clientHeader = [
      `gax/${this._gaxModule.version}`,
      `gapic/${version}`,
    ];
    if (typeof process !== 'undefined' && 'versions' in process) {
      clientHeader.push(`gl-node/${process.versions.node}`);
    } else {
      clientHeader.push(`gl-web/${this._gaxModule.version}`);
    }
    if (!opts.fallback) {
      clientHeader.push(`grpc/${this._gaxGrpc.grpcVersion}`);
    }
    if (opts.libName && opts.libVersion) {
      clientHeader.push(`${opts.libName}/${opts.libVersion}`);
    }
    // Load the applicable protos.
    // For Node.js, pass the path to JSON proto file.
    // For browsers, pass the JSON content.

    const nodejsProtoPath = path.join(__dirname, '..', '..', 'protos', 'protos.json');
    this._protos = this._gaxGrpc.loadProto(
      opts.fallback ?
        // eslint-disable-next-line @typescript-eslint/no-var-requires
        require("../../protos/protos.json") :
        nodejsProtoPath
    );

    // This API contains "path templates"; forward-slash-separated
    // identifiers to uniquely identify resources within the API.
    // Create useful helper objects for these.
    this.pathTemplates = {
      devicePathTemplate: new this._gaxModule.PathTemplate(
        'enterprises/{enterprise}/devices/{device}'
      ),
      roomPathTemplate: new this._gaxModule.PathTemplate(
        'enterprises/{enterprise}/structures/{structure}/rooms/{room}'
      ),
      structurePathTemplate: new this._gaxModule.PathTemplate(
        'enterprises/{enterprise}/structures/{structure}'
      ),
    };

    // Some of the methods on this service return "paged" results,
    // (e.g. 50 results at a time, with tokens to get subsequent
    // pages). Denote the keys used for pagination and results.
    this.descriptors.page = {
      listDevices:
          new this._gaxModule.PageDescriptor('pageToken', 'nextPageToken', 'devices'),
      listStructures:
          new this._gaxModule.PageDescriptor('pageToken', 'nextPageToken', 'structures'),
      listRooms:
          new this._gaxModule.PageDescriptor('pageToken', 'nextPageToken', 'rooms')
    };

    // Put together the default options sent with requests.
    this._defaults = this._gaxGrpc.constructSettings(
        'google.home.enterprise.sdm.v1.SmartDeviceManagementService', gapicConfig as gax.ClientConfig,
        opts.clientConfig || {}, {'x-goog-api-client': clientHeader.join(' ')});

    // Set up a dictionary of "inner API calls"; the core implementation
    // of calling the API is handled in `google-gax`, with this code
    // merely providing the destination and request information.
    this.innerApiCalls = {};
  }

  /**
   * Initialize the client.
   * Performs asynchronous operations (such as authentication) and prepares the client.
   * This function will be called automatically when any class method is called for the
   * first time, but if you need to initialize it before calling an actual method,
   * feel free to call initialize() directly.
   *
   * You can await on this method if you want to make sure the client is initialized.
   *
   * @returns {Promise} A promise that resolves to an authenticated service stub.
   */
  initialize() {
    // If the client stub promise is already initialized, return immediately.
    if (this.smartDeviceManagementServiceStub) {
      return this.smartDeviceManagementServiceStub;
    }

    // Put together the "service stub" for
    // google.home.enterprise.sdm.v1.SmartDeviceManagementService.
    this.smartDeviceManagementServiceStub = this._gaxGrpc.createStub(
        this._opts.fallback ?
          (this._protos as protobuf.Root).lookupService('google.home.enterprise.sdm.v1.SmartDeviceManagementService') :
          // eslint-disable-next-line @typescript-eslint/no-explicit-any
          (this._protos as any).google.home.enterprise.sdm.v1.SmartDeviceManagementService,
        this._opts) as Promise<{[method: string]: Function}>;

    // Iterate over each of the methods that the service provides
    // and create an API call method for each.
    const smartDeviceManagementServiceStubMethods =
        ['getDevice', 'listDevices', 'executeDeviceCommand', 'getStructure', 'listStructures', 'getRoom', 'listRooms'];
    for (const methodName of smartDeviceManagementServiceStubMethods) {
      const callPromise = this.smartDeviceManagementServiceStub.then(
        stub => (...args: Array<{}>) => {
          if (this._terminated) {
            return Promise.reject('The client has already been closed.');
          }
          const func = stub[methodName];
          return func.apply(stub, args);
        },
        (err: Error|null|undefined) => () => {
          throw err;
        });

      const descriptor =
        this.descriptors.page[methodName] ||
        undefined;
      const apiCall = this._gaxModule.createApiCall(
        callPromise,
        this._defaults[methodName],
        descriptor
      );

      this.innerApiCalls[methodName] = apiCall;
    }

    return this.smartDeviceManagementServiceStub;
  }

  /**
   * The DNS address for this API service.
   * @returns {string} The DNS address for this service.
   */
  static get servicePath() {
    return 'smartdevicemanagement.googleapis.com';
  }

  /**
   * The DNS address for this API service - same as servicePath(),
   * exists for compatibility reasons.
   * @returns {string} The DNS address for this service.
   */
  static get apiEndpoint() {
    return 'smartdevicemanagement.googleapis.com';
  }

  /**
   * The port for this API service.
   * @returns {number} The default port for this service.
   */
  static get port() {
    return 443;
  }

  /**
   * The scopes needed to make gRPC calls for every method defined
   * in this service.
   * @returns {string[]} List of default scopes.
   */
  static get scopes() {
    return [
      'https://www.googleapis.com/auth/sdm.service'
    ];
  }

  getProjectId(): Promise<string>;
  getProjectId(callback: Callback<string, undefined, undefined>): void;
  /**
   * Return the project ID used by this class.
   * @returns {Promise} A promise that resolves to string containing the project ID.
   */
  getProjectId(callback?: Callback<string, undefined, undefined>):
      Promise<string>|void {
    if (callback) {
      this.auth.getProjectId(callback);
      return;
    }
    return this.auth.getProjectId();
  }

  // -------------------
  // -- Service calls --
  // -------------------
  getDevice(
      request: protos.google.home.enterprise.sdm.v1.IGetDeviceRequest,
      options?: CallOptions):
      Promise<[
        protos.google.home.enterprise.sdm.v1.IDevice,
        protos.google.home.enterprise.sdm.v1.IGetDeviceRequest|undefined, {}|undefined
      ]>;
  getDevice(
      request: protos.google.home.enterprise.sdm.v1.IGetDeviceRequest,
      options: CallOptions,
      callback: Callback<
          protos.google.home.enterprise.sdm.v1.IDevice,
          protos.google.home.enterprise.sdm.v1.IGetDeviceRequest|null|undefined,
          {}|null|undefined>): void;
  getDevice(
      request: protos.google.home.enterprise.sdm.v1.IGetDeviceRequest,
      callback: Callback<
          protos.google.home.enterprise.sdm.v1.IDevice,
          protos.google.home.enterprise.sdm.v1.IGetDeviceRequest|null|undefined,
          {}|null|undefined>): void;
/**
 * Gets a device managed by the enterprise.
 *
 * @param {Object} request
 *   The request object that will be sent.
 * @param {string} request.name
 *   The name of the device requested. For example:
 *   "enterprises/XYZ/devices/123"
 * @param {object} [options]
 *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
 * @returns {Promise} - The promise which resolves to an array.
 *   The first element of the array is an object representing [Device]{@link google.home.enterprise.sdm.v1.Device}.
 *   Please see the
 *   [documentation](https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods)
 *   for more details and examples.
 * @example
 * const [response] = await client.getDevice(request);
 */
  getDevice(
      request: protos.google.home.enterprise.sdm.v1.IGetDeviceRequest,
      optionsOrCallback?: CallOptions|Callback<
          protos.google.home.enterprise.sdm.v1.IDevice,
          protos.google.home.enterprise.sdm.v1.IGetDeviceRequest|null|undefined,
          {}|null|undefined>,
      callback?: Callback<
          protos.google.home.enterprise.sdm.v1.IDevice,
          protos.google.home.enterprise.sdm.v1.IGetDeviceRequest|null|undefined,
          {}|null|undefined>):
      Promise<[
        protos.google.home.enterprise.sdm.v1.IDevice,
        protos.google.home.enterprise.sdm.v1.IGetDeviceRequest|undefined, {}|undefined
      ]>|void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    }
    else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers[
      'x-goog-request-params'
    ] = gax.routingHeader.fromParams({
      'name': request.name || '',
    });
    this.initialize();
    return this.innerApiCalls.getDevice(request, options, callback);
  }
  executeDeviceCommand(
      request: protos.google.home.enterprise.sdm.v1.IExecuteDeviceCommandRequest,
      options?: CallOptions):
      Promise<[
        protos.google.home.enterprise.sdm.v1.IExecuteDeviceCommandResponse,
        protos.google.home.enterprise.sdm.v1.IExecuteDeviceCommandRequest|undefined, {}|undefined
      ]>;
  executeDeviceCommand(
      request: protos.google.home.enterprise.sdm.v1.IExecuteDeviceCommandRequest,
      options: CallOptions,
      callback: Callback<
          protos.google.home.enterprise.sdm.v1.IExecuteDeviceCommandResponse,
          protos.google.home.enterprise.sdm.v1.IExecuteDeviceCommandRequest|null|undefined,
          {}|null|undefined>): void;
  executeDeviceCommand(
      request: protos.google.home.enterprise.sdm.v1.IExecuteDeviceCommandRequest,
      callback: Callback<
          protos.google.home.enterprise.sdm.v1.IExecuteDeviceCommandResponse,
          protos.google.home.enterprise.sdm.v1.IExecuteDeviceCommandRequest|null|undefined,
          {}|null|undefined>): void;
/**
 * Executes a command to device managed by the enterprise.
 *
 * @param {Object} request
 *   The request object that will be sent.
 * @param {string} request.name
 *   The name of the device requested. For example:
 *   "enterprises/XYZ/devices/123"
 * @param {string} request.command
 *   The command name to execute, represented by the fully qualified protobuf
 *   message name.
 * @param {google.protobuf.Struct} request.params
 *   The command message to execute, represented as a Struct.
 * @param {object} [options]
 *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
 * @returns {Promise} - The promise which resolves to an array.
 *   The first element of the array is an object representing [ExecuteDeviceCommandResponse]{@link google.home.enterprise.sdm.v1.ExecuteDeviceCommandResponse}.
 *   Please see the
 *   [documentation](https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods)
 *   for more details and examples.
 * @example
 * const [response] = await client.executeDeviceCommand(request);
 */
  executeDeviceCommand(
      request: protos.google.home.enterprise.sdm.v1.IExecuteDeviceCommandRequest,
      optionsOrCallback?: CallOptions|Callback<
          protos.google.home.enterprise.sdm.v1.IExecuteDeviceCommandResponse,
          protos.google.home.enterprise.sdm.v1.IExecuteDeviceCommandRequest|null|undefined,
          {}|null|undefined>,
      callback?: Callback<
          protos.google.home.enterprise.sdm.v1.IExecuteDeviceCommandResponse,
          protos.google.home.enterprise.sdm.v1.IExecuteDeviceCommandRequest|null|undefined,
          {}|null|undefined>):
      Promise<[
        protos.google.home.enterprise.sdm.v1.IExecuteDeviceCommandResponse,
        protos.google.home.enterprise.sdm.v1.IExecuteDeviceCommandRequest|undefined, {}|undefined
      ]>|void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    }
    else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers[
      'x-goog-request-params'
    ] = gax.routingHeader.fromParams({
      'name': request.name || '',
    });
    this.initialize();
    return this.innerApiCalls.executeDeviceCommand(request, options, callback);
  }
  getStructure(
      request: protos.google.home.enterprise.sdm.v1.IGetStructureRequest,
      options?: CallOptions):
      Promise<[
        protos.google.home.enterprise.sdm.v1.IStructure,
        protos.google.home.enterprise.sdm.v1.IGetStructureRequest|undefined, {}|undefined
      ]>;
  getStructure(
      request: protos.google.home.enterprise.sdm.v1.IGetStructureRequest,
      options: CallOptions,
      callback: Callback<
          protos.google.home.enterprise.sdm.v1.IStructure,
          protos.google.home.enterprise.sdm.v1.IGetStructureRequest|null|undefined,
          {}|null|undefined>): void;
  getStructure(
      request: protos.google.home.enterprise.sdm.v1.IGetStructureRequest,
      callback: Callback<
          protos.google.home.enterprise.sdm.v1.IStructure,
          protos.google.home.enterprise.sdm.v1.IGetStructureRequest|null|undefined,
          {}|null|undefined>): void;
/**
 * Gets a structure managed by the enterprise.
 *
 * @param {Object} request
 *   The request object that will be sent.
 * @param {string} request.name
 *   The name of the structure requested. For example:
 *   "enterprises/XYZ/structures/ABC".
 * @param {object} [options]
 *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
 * @returns {Promise} - The promise which resolves to an array.
 *   The first element of the array is an object representing [Structure]{@link google.home.enterprise.sdm.v1.Structure}.
 *   Please see the
 *   [documentation](https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods)
 *   for more details and examples.
 * @example
 * const [response] = await client.getStructure(request);
 */
  getStructure(
      request: protos.google.home.enterprise.sdm.v1.IGetStructureRequest,
      optionsOrCallback?: CallOptions|Callback<
          protos.google.home.enterprise.sdm.v1.IStructure,
          protos.google.home.enterprise.sdm.v1.IGetStructureRequest|null|undefined,
          {}|null|undefined>,
      callback?: Callback<
          protos.google.home.enterprise.sdm.v1.IStructure,
          protos.google.home.enterprise.sdm.v1.IGetStructureRequest|null|undefined,
          {}|null|undefined>):
      Promise<[
        protos.google.home.enterprise.sdm.v1.IStructure,
        protos.google.home.enterprise.sdm.v1.IGetStructureRequest|undefined, {}|undefined
      ]>|void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    }
    else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers[
      'x-goog-request-params'
    ] = gax.routingHeader.fromParams({
      'name': request.name || '',
    });
    this.initialize();
    return this.innerApiCalls.getStructure(request, options, callback);
  }
  getRoom(
      request: protos.google.home.enterprise.sdm.v1.IGetRoomRequest,
      options?: CallOptions):
      Promise<[
        protos.google.home.enterprise.sdm.v1.IRoom,
        protos.google.home.enterprise.sdm.v1.IGetRoomRequest|undefined, {}|undefined
      ]>;
  getRoom(
      request: protos.google.home.enterprise.sdm.v1.IGetRoomRequest,
      options: CallOptions,
      callback: Callback<
          protos.google.home.enterprise.sdm.v1.IRoom,
          protos.google.home.enterprise.sdm.v1.IGetRoomRequest|null|undefined,
          {}|null|undefined>): void;
  getRoom(
      request: protos.google.home.enterprise.sdm.v1.IGetRoomRequest,
      callback: Callback<
          protos.google.home.enterprise.sdm.v1.IRoom,
          protos.google.home.enterprise.sdm.v1.IGetRoomRequest|null|undefined,
          {}|null|undefined>): void;
/**
 * Gets a room managed by the enterprise.
 *
 * @param {Object} request
 *   The request object that will be sent.
 * @param {string} request.name
 *   The name of the room requested. For example:
 *   "enterprises/XYZ/structures/ABC/rooms/123".
 * @param {object} [options]
 *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
 * @returns {Promise} - The promise which resolves to an array.
 *   The first element of the array is an object representing [Room]{@link google.home.enterprise.sdm.v1.Room}.
 *   Please see the
 *   [documentation](https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods)
 *   for more details and examples.
 * @example
 * const [response] = await client.getRoom(request);
 */
  getRoom(
      request: protos.google.home.enterprise.sdm.v1.IGetRoomRequest,
      optionsOrCallback?: CallOptions|Callback<
          protos.google.home.enterprise.sdm.v1.IRoom,
          protos.google.home.enterprise.sdm.v1.IGetRoomRequest|null|undefined,
          {}|null|undefined>,
      callback?: Callback<
          protos.google.home.enterprise.sdm.v1.IRoom,
          protos.google.home.enterprise.sdm.v1.IGetRoomRequest|null|undefined,
          {}|null|undefined>):
      Promise<[
        protos.google.home.enterprise.sdm.v1.IRoom,
        protos.google.home.enterprise.sdm.v1.IGetRoomRequest|undefined, {}|undefined
      ]>|void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    }
    else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers[
      'x-goog-request-params'
    ] = gax.routingHeader.fromParams({
      'name': request.name || '',
    });
    this.initialize();
    return this.innerApiCalls.getRoom(request, options, callback);
  }

  listDevices(
      request: protos.google.home.enterprise.sdm.v1.IListDevicesRequest,
      options?: CallOptions):
      Promise<[
        protos.google.home.enterprise.sdm.v1.IDevice[],
        protos.google.home.enterprise.sdm.v1.IListDevicesRequest|null,
        protos.google.home.enterprise.sdm.v1.IListDevicesResponse
      ]>;
  listDevices(
      request: protos.google.home.enterprise.sdm.v1.IListDevicesRequest,
      options: CallOptions,
      callback: PaginationCallback<
          protos.google.home.enterprise.sdm.v1.IListDevicesRequest,
          protos.google.home.enterprise.sdm.v1.IListDevicesResponse|null|undefined,
          protos.google.home.enterprise.sdm.v1.IDevice>): void;
  listDevices(
      request: protos.google.home.enterprise.sdm.v1.IListDevicesRequest,
      callback: PaginationCallback<
          protos.google.home.enterprise.sdm.v1.IListDevicesRequest,
          protos.google.home.enterprise.sdm.v1.IListDevicesResponse|null|undefined,
          protos.google.home.enterprise.sdm.v1.IDevice>): void;
/**
 * Lists devices managed by the enterprise.
 *
 * @param {Object} request
 *   The request object that will be sent.
 * @param {string} request.parent
 *   The parent enterprise to list devices under. E.g. "enterprises/XYZ".
 * @param {number} request.pageSize
 *   Optional requested page size. Server may return fewer devices than
 *   requested. If unspecified, server will pick an appropriate default.
 * @param {string} request.pageToken
 *   Optional token of the page to retrieve.
 * @param {string} request.filter
 *   Optional filter to list devices.
 *
 *   Filters can match the exact parent (could be a structure or a room):
 *   'parent=enterprises/XYZ/structures/jkl'
 *   or filter by device custom name (substring match):
 *   'customName=wing'
 * @param {object} [options]
 *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
 * @returns {Promise} - The promise which resolves to an array.
 *   The first element of the array is Array of [Device]{@link google.home.enterprise.sdm.v1.Device}.
 *   The client library will perform auto-pagination by default: it will call the API as many
 *   times as needed and will merge results from all the pages into this array.
 *   Note that it can affect your quota.
 *   We recommend using `listDevicesAsync()`
 *   method described below for async iteration which you can stop as needed.
 *   Please see the
 *   [documentation](https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination)
 *   for more details and examples.
 */
  listDevices(
      request: protos.google.home.enterprise.sdm.v1.IListDevicesRequest,
      optionsOrCallback?: CallOptions|PaginationCallback<
          protos.google.home.enterprise.sdm.v1.IListDevicesRequest,
          protos.google.home.enterprise.sdm.v1.IListDevicesResponse|null|undefined,
          protos.google.home.enterprise.sdm.v1.IDevice>,
      callback?: PaginationCallback<
          protos.google.home.enterprise.sdm.v1.IListDevicesRequest,
          protos.google.home.enterprise.sdm.v1.IListDevicesResponse|null|undefined,
          protos.google.home.enterprise.sdm.v1.IDevice>):
      Promise<[
        protos.google.home.enterprise.sdm.v1.IDevice[],
        protos.google.home.enterprise.sdm.v1.IListDevicesRequest|null,
        protos.google.home.enterprise.sdm.v1.IListDevicesResponse
      ]>|void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    }
    else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers[
      'x-goog-request-params'
    ] = gax.routingHeader.fromParams({
      'parent': request.parent || '',
    });
    this.initialize();
    return this.innerApiCalls.listDevices(request, options, callback);
  }

/**
 * Equivalent to `method.name.toCamelCase()`, but returns a NodeJS Stream object.
 * @param {Object} request
 *   The request object that will be sent.
 * @param {string} request.parent
 *   The parent enterprise to list devices under. E.g. "enterprises/XYZ".
 * @param {number} request.pageSize
 *   Optional requested page size. Server may return fewer devices than
 *   requested. If unspecified, server will pick an appropriate default.
 * @param {string} request.pageToken
 *   Optional token of the page to retrieve.
 * @param {string} request.filter
 *   Optional filter to list devices.
 *
 *   Filters can match the exact parent (could be a structure or a room):
 *   'parent=enterprises/XYZ/structures/jkl'
 *   or filter by device custom name (substring match):
 *   'customName=wing'
 * @param {object} [options]
 *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
 * @returns {Stream}
 *   An object stream which emits an object representing [Device]{@link google.home.enterprise.sdm.v1.Device} on 'data' event.
 *   The client library will perform auto-pagination by default: it will call the API as many
 *   times as needed. Note that it can affect your quota.
 *   We recommend using `listDevicesAsync()`
 *   method described below for async iteration which you can stop as needed.
 *   Please see the
 *   [documentation](https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination)
 *   for more details and examples.
 */
  listDevicesStream(
      request?: protos.google.home.enterprise.sdm.v1.IListDevicesRequest,
      options?: CallOptions):
    Transform{
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers[
      'x-goog-request-params'
    ] = gax.routingHeader.fromParams({
      'parent': request.parent || '',
    });
    const callSettings = new gax.CallSettings(options);
    this.initialize();
    return this.descriptors.page.listDevices.createStream(
      this.innerApiCalls.listDevices as gax.GaxCall,
      request,
      callSettings
    );
  }

/**
 * Equivalent to `listDevices`, but returns an iterable object.
 *
 * `for`-`await`-`of` syntax is used with the iterable to get response elements on-demand.
 * @param {Object} request
 *   The request object that will be sent.
 * @param {string} request.parent
 *   The parent enterprise to list devices under. E.g. "enterprises/XYZ".
 * @param {number} request.pageSize
 *   Optional requested page size. Server may return fewer devices than
 *   requested. If unspecified, server will pick an appropriate default.
 * @param {string} request.pageToken
 *   Optional token of the page to retrieve.
 * @param {string} request.filter
 *   Optional filter to list devices.
 *
 *   Filters can match the exact parent (could be a structure or a room):
 *   'parent=enterprises/XYZ/structures/jkl'
 *   or filter by device custom name (substring match):
 *   'customName=wing'
 * @param {object} [options]
 *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
 * @returns {Object}
 *   An iterable Object that allows [async iteration](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Iteration_protocols).
 *   When you iterate the returned iterable, each element will be an object representing
 *   [Device]{@link google.home.enterprise.sdm.v1.Device}. The API will be called under the hood as needed, once per the page,
 *   so you can stop the iteration when you don't need more results.
 *   Please see the
 *   [documentation](https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination)
 *   for more details and examples.
 * @example
 * const iterable = client.listDevicesAsync(request);
 * for await (const response of iterable) {
 *   // process response
 * }
 */
  listDevicesAsync(
      request?: protos.google.home.enterprise.sdm.v1.IListDevicesRequest,
      options?: CallOptions):
    AsyncIterable<protos.google.home.enterprise.sdm.v1.IDevice>{
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers[
      'x-goog-request-params'
    ] = gax.routingHeader.fromParams({
      'parent': request.parent || '',
    });
    options = options || {};
    const callSettings = new gax.CallSettings(options);
    this.initialize();
    return this.descriptors.page.listDevices.asyncIterate(
      this.innerApiCalls['listDevices'] as GaxCall,
      request as unknown as RequestType,
      callSettings
    ) as AsyncIterable<protos.google.home.enterprise.sdm.v1.IDevice>;
  }
  listStructures(
      request: protos.google.home.enterprise.sdm.v1.IListStructuresRequest,
      options?: CallOptions):
      Promise<[
        protos.google.home.enterprise.sdm.v1.IStructure[],
        protos.google.home.enterprise.sdm.v1.IListStructuresRequest|null,
        protos.google.home.enterprise.sdm.v1.IListStructuresResponse
      ]>;
  listStructures(
      request: protos.google.home.enterprise.sdm.v1.IListStructuresRequest,
      options: CallOptions,
      callback: PaginationCallback<
          protos.google.home.enterprise.sdm.v1.IListStructuresRequest,
          protos.google.home.enterprise.sdm.v1.IListStructuresResponse|null|undefined,
          protos.google.home.enterprise.sdm.v1.IStructure>): void;
  listStructures(
      request: protos.google.home.enterprise.sdm.v1.IListStructuresRequest,
      callback: PaginationCallback<
          protos.google.home.enterprise.sdm.v1.IListStructuresRequest,
          protos.google.home.enterprise.sdm.v1.IListStructuresResponse|null|undefined,
          protos.google.home.enterprise.sdm.v1.IStructure>): void;
/**
 * Lists structures managed by the enterprise.
 *
 * @param {Object} request
 *   The request object that will be sent.
 * @param {string} request.parent
 *   The parent enterprise to list structures under. E.g. "enterprises/XYZ".
 * @param {number} request.pageSize
 *   Requested page size. Server may return fewer structures than requested.
 *   If unspecified, server will pick an appropriate default.
 * @param {string} request.pageToken
 *   The token of the page to retrieve.
 * @param {string} request.filter
 *   Optional filter to list structures.
 * @param {object} [options]
 *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
 * @returns {Promise} - The promise which resolves to an array.
 *   The first element of the array is Array of [Structure]{@link google.home.enterprise.sdm.v1.Structure}.
 *   The client library will perform auto-pagination by default: it will call the API as many
 *   times as needed and will merge results from all the pages into this array.
 *   Note that it can affect your quota.
 *   We recommend using `listStructuresAsync()`
 *   method described below for async iteration which you can stop as needed.
 *   Please see the
 *   [documentation](https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination)
 *   for more details and examples.
 */
  listStructures(
      request: protos.google.home.enterprise.sdm.v1.IListStructuresRequest,
      optionsOrCallback?: CallOptions|PaginationCallback<
          protos.google.home.enterprise.sdm.v1.IListStructuresRequest,
          protos.google.home.enterprise.sdm.v1.IListStructuresResponse|null|undefined,
          protos.google.home.enterprise.sdm.v1.IStructure>,
      callback?: PaginationCallback<
          protos.google.home.enterprise.sdm.v1.IListStructuresRequest,
          protos.google.home.enterprise.sdm.v1.IListStructuresResponse|null|undefined,
          protos.google.home.enterprise.sdm.v1.IStructure>):
      Promise<[
        protos.google.home.enterprise.sdm.v1.IStructure[],
        protos.google.home.enterprise.sdm.v1.IListStructuresRequest|null,
        protos.google.home.enterprise.sdm.v1.IListStructuresResponse
      ]>|void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    }
    else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers[
      'x-goog-request-params'
    ] = gax.routingHeader.fromParams({
      'parent': request.parent || '',
    });
    this.initialize();
    return this.innerApiCalls.listStructures(request, options, callback);
  }

/**
 * Equivalent to `method.name.toCamelCase()`, but returns a NodeJS Stream object.
 * @param {Object} request
 *   The request object that will be sent.
 * @param {string} request.parent
 *   The parent enterprise to list structures under. E.g. "enterprises/XYZ".
 * @param {number} request.pageSize
 *   Requested page size. Server may return fewer structures than requested.
 *   If unspecified, server will pick an appropriate default.
 * @param {string} request.pageToken
 *   The token of the page to retrieve.
 * @param {string} request.filter
 *   Optional filter to list structures.
 * @param {object} [options]
 *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
 * @returns {Stream}
 *   An object stream which emits an object representing [Structure]{@link google.home.enterprise.sdm.v1.Structure} on 'data' event.
 *   The client library will perform auto-pagination by default: it will call the API as many
 *   times as needed. Note that it can affect your quota.
 *   We recommend using `listStructuresAsync()`
 *   method described below for async iteration which you can stop as needed.
 *   Please see the
 *   [documentation](https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination)
 *   for more details and examples.
 */
  listStructuresStream(
      request?: protos.google.home.enterprise.sdm.v1.IListStructuresRequest,
      options?: CallOptions):
    Transform{
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers[
      'x-goog-request-params'
    ] = gax.routingHeader.fromParams({
      'parent': request.parent || '',
    });
    const callSettings = new gax.CallSettings(options);
    this.initialize();
    return this.descriptors.page.listStructures.createStream(
      this.innerApiCalls.listStructures as gax.GaxCall,
      request,
      callSettings
    );
  }

/**
 * Equivalent to `listStructures`, but returns an iterable object.
 *
 * `for`-`await`-`of` syntax is used with the iterable to get response elements on-demand.
 * @param {Object} request
 *   The request object that will be sent.
 * @param {string} request.parent
 *   The parent enterprise to list structures under. E.g. "enterprises/XYZ".
 * @param {number} request.pageSize
 *   Requested page size. Server may return fewer structures than requested.
 *   If unspecified, server will pick an appropriate default.
 * @param {string} request.pageToken
 *   The token of the page to retrieve.
 * @param {string} request.filter
 *   Optional filter to list structures.
 * @param {object} [options]
 *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
 * @returns {Object}
 *   An iterable Object that allows [async iteration](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Iteration_protocols).
 *   When you iterate the returned iterable, each element will be an object representing
 *   [Structure]{@link google.home.enterprise.sdm.v1.Structure}. The API will be called under the hood as needed, once per the page,
 *   so you can stop the iteration when you don't need more results.
 *   Please see the
 *   [documentation](https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination)
 *   for more details and examples.
 * @example
 * const iterable = client.listStructuresAsync(request);
 * for await (const response of iterable) {
 *   // process response
 * }
 */
  listStructuresAsync(
      request?: protos.google.home.enterprise.sdm.v1.IListStructuresRequest,
      options?: CallOptions):
    AsyncIterable<protos.google.home.enterprise.sdm.v1.IStructure>{
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers[
      'x-goog-request-params'
    ] = gax.routingHeader.fromParams({
      'parent': request.parent || '',
    });
    options = options || {};
    const callSettings = new gax.CallSettings(options);
    this.initialize();
    return this.descriptors.page.listStructures.asyncIterate(
      this.innerApiCalls['listStructures'] as GaxCall,
      request as unknown as RequestType,
      callSettings
    ) as AsyncIterable<protos.google.home.enterprise.sdm.v1.IStructure>;
  }
  listRooms(
      request: protos.google.home.enterprise.sdm.v1.IListRoomsRequest,
      options?: CallOptions):
      Promise<[
        protos.google.home.enterprise.sdm.v1.IRoom[],
        protos.google.home.enterprise.sdm.v1.IListRoomsRequest|null,
        protos.google.home.enterprise.sdm.v1.IListRoomsResponse
      ]>;
  listRooms(
      request: protos.google.home.enterprise.sdm.v1.IListRoomsRequest,
      options: CallOptions,
      callback: PaginationCallback<
          protos.google.home.enterprise.sdm.v1.IListRoomsRequest,
          protos.google.home.enterprise.sdm.v1.IListRoomsResponse|null|undefined,
          protos.google.home.enterprise.sdm.v1.IRoom>): void;
  listRooms(
      request: protos.google.home.enterprise.sdm.v1.IListRoomsRequest,
      callback: PaginationCallback<
          protos.google.home.enterprise.sdm.v1.IListRoomsRequest,
          protos.google.home.enterprise.sdm.v1.IListRoomsResponse|null|undefined,
          protos.google.home.enterprise.sdm.v1.IRoom>): void;
/**
 * Lists rooms managed by the enterprise.
 *
 * @param {Object} request
 *   The request object that will be sent.
 * @param {string} request.parent
 *   The parent resource name of the rooms requested. For example:
 *   "enterprises/XYZ/structures/ABC".
 * @param {number} request.pageSize
 *   Requested page size. Server may return fewer rooms than requested.
 *   If unspecified, server will pick an appropriate default.
 * @param {string} request.pageToken
 *   The token of the page to retrieve.
 * @param {object} [options]
 *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
 * @returns {Promise} - The promise which resolves to an array.
 *   The first element of the array is Array of [Room]{@link google.home.enterprise.sdm.v1.Room}.
 *   The client library will perform auto-pagination by default: it will call the API as many
 *   times as needed and will merge results from all the pages into this array.
 *   Note that it can affect your quota.
 *   We recommend using `listRoomsAsync()`
 *   method described below for async iteration which you can stop as needed.
 *   Please see the
 *   [documentation](https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination)
 *   for more details and examples.
 */
  listRooms(
      request: protos.google.home.enterprise.sdm.v1.IListRoomsRequest,
      optionsOrCallback?: CallOptions|PaginationCallback<
          protos.google.home.enterprise.sdm.v1.IListRoomsRequest,
          protos.google.home.enterprise.sdm.v1.IListRoomsResponse|null|undefined,
          protos.google.home.enterprise.sdm.v1.IRoom>,
      callback?: PaginationCallback<
          protos.google.home.enterprise.sdm.v1.IListRoomsRequest,
          protos.google.home.enterprise.sdm.v1.IListRoomsResponse|null|undefined,
          protos.google.home.enterprise.sdm.v1.IRoom>):
      Promise<[
        protos.google.home.enterprise.sdm.v1.IRoom[],
        protos.google.home.enterprise.sdm.v1.IListRoomsRequest|null,
        protos.google.home.enterprise.sdm.v1.IListRoomsResponse
      ]>|void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    }
    else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers[
      'x-goog-request-params'
    ] = gax.routingHeader.fromParams({
      'parent': request.parent || '',
    });
    this.initialize();
    return this.innerApiCalls.listRooms(request, options, callback);
  }

/**
 * Equivalent to `method.name.toCamelCase()`, but returns a NodeJS Stream object.
 * @param {Object} request
 *   The request object that will be sent.
 * @param {string} request.parent
 *   The parent resource name of the rooms requested. For example:
 *   "enterprises/XYZ/structures/ABC".
 * @param {number} request.pageSize
 *   Requested page size. Server may return fewer rooms than requested.
 *   If unspecified, server will pick an appropriate default.
 * @param {string} request.pageToken
 *   The token of the page to retrieve.
 * @param {object} [options]
 *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
 * @returns {Stream}
 *   An object stream which emits an object representing [Room]{@link google.home.enterprise.sdm.v1.Room} on 'data' event.
 *   The client library will perform auto-pagination by default: it will call the API as many
 *   times as needed. Note that it can affect your quota.
 *   We recommend using `listRoomsAsync()`
 *   method described below for async iteration which you can stop as needed.
 *   Please see the
 *   [documentation](https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination)
 *   for more details and examples.
 */
  listRoomsStream(
      request?: protos.google.home.enterprise.sdm.v1.IListRoomsRequest,
      options?: CallOptions):
    Transform{
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers[
      'x-goog-request-params'
    ] = gax.routingHeader.fromParams({
      'parent': request.parent || '',
    });
    const callSettings = new gax.CallSettings(options);
    this.initialize();
    return this.descriptors.page.listRooms.createStream(
      this.innerApiCalls.listRooms as gax.GaxCall,
      request,
      callSettings
    );
  }

/**
 * Equivalent to `listRooms`, but returns an iterable object.
 *
 * `for`-`await`-`of` syntax is used with the iterable to get response elements on-demand.
 * @param {Object} request
 *   The request object that will be sent.
 * @param {string} request.parent
 *   The parent resource name of the rooms requested. For example:
 *   "enterprises/XYZ/structures/ABC".
 * @param {number} request.pageSize
 *   Requested page size. Server may return fewer rooms than requested.
 *   If unspecified, server will pick an appropriate default.
 * @param {string} request.pageToken
 *   The token of the page to retrieve.
 * @param {object} [options]
 *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
 * @returns {Object}
 *   An iterable Object that allows [async iteration](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Iteration_protocols).
 *   When you iterate the returned iterable, each element will be an object representing
 *   [Room]{@link google.home.enterprise.sdm.v1.Room}. The API will be called under the hood as needed, once per the page,
 *   so you can stop the iteration when you don't need more results.
 *   Please see the
 *   [documentation](https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination)
 *   for more details and examples.
 * @example
 * const iterable = client.listRoomsAsync(request);
 * for await (const response of iterable) {
 *   // process response
 * }
 */
  listRoomsAsync(
      request?: protos.google.home.enterprise.sdm.v1.IListRoomsRequest,
      options?: CallOptions):
    AsyncIterable<protos.google.home.enterprise.sdm.v1.IRoom>{
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers[
      'x-goog-request-params'
    ] = gax.routingHeader.fromParams({
      'parent': request.parent || '',
    });
    options = options || {};
    const callSettings = new gax.CallSettings(options);
    this.initialize();
    return this.descriptors.page.listRooms.asyncIterate(
      this.innerApiCalls['listRooms'] as GaxCall,
      request as unknown as RequestType,
      callSettings
    ) as AsyncIterable<protos.google.home.enterprise.sdm.v1.IRoom>;
  }
  // --------------------
  // -- Path templates --
  // --------------------

  /**
   * Return a fully-qualified device resource name string.
   *
   * @param {string} enterprise
   * @param {string} device
   * @returns {string} Resource name string.
   */
  devicePath(enterprise:string,device:string) {
    return this.pathTemplates.devicePathTemplate.render({
      enterprise: enterprise,
      device: device,
    });
  }

  /**
   * Parse the enterprise from Device resource.
   *
   * @param {string} deviceName
   *   A fully-qualified path representing Device resource.
   * @returns {string} A string representing the enterprise.
   */
  matchEnterpriseFromDeviceName(deviceName: string) {
    return this.pathTemplates.devicePathTemplate.match(deviceName).enterprise;
  }

  /**
   * Parse the device from Device resource.
   *
   * @param {string} deviceName
   *   A fully-qualified path representing Device resource.
   * @returns {string} A string representing the device.
   */
  matchDeviceFromDeviceName(deviceName: string) {
    return this.pathTemplates.devicePathTemplate.match(deviceName).device;
  }

  /**
   * Return a fully-qualified room resource name string.
   *
   * @param {string} enterprise
   * @param {string} structure
   * @param {string} room
   * @returns {string} Resource name string.
   */
  roomPath(enterprise:string,structure:string,room:string) {
    return this.pathTemplates.roomPathTemplate.render({
      enterprise: enterprise,
      structure: structure,
      room: room,
    });
  }

  /**
   * Parse the enterprise from Room resource.
   *
   * @param {string} roomName
   *   A fully-qualified path representing Room resource.
   * @returns {string} A string representing the enterprise.
   */
  matchEnterpriseFromRoomName(roomName: string) {
    return this.pathTemplates.roomPathTemplate.match(roomName).enterprise;
  }

  /**
   * Parse the structure from Room resource.
   *
   * @param {string} roomName
   *   A fully-qualified path representing Room resource.
   * @returns {string} A string representing the structure.
   */
  matchStructureFromRoomName(roomName: string) {
    return this.pathTemplates.roomPathTemplate.match(roomName).structure;
  }

  /**
   * Parse the room from Room resource.
   *
   * @param {string} roomName
   *   A fully-qualified path representing Room resource.
   * @returns {string} A string representing the room.
   */
  matchRoomFromRoomName(roomName: string) {
    return this.pathTemplates.roomPathTemplate.match(roomName).room;
  }

  /**
   * Return a fully-qualified structure resource name string.
   *
   * @param {string} enterprise
   * @param {string} structure
   * @returns {string} Resource name string.
   */
  structurePath(enterprise:string,structure:string) {
    return this.pathTemplates.structurePathTemplate.render({
      enterprise: enterprise,
      structure: structure,
    });
  }

  /**
   * Parse the enterprise from Structure resource.
   *
   * @param {string} structureName
   *   A fully-qualified path representing Structure resource.
   * @returns {string} A string representing the enterprise.
   */
  matchEnterpriseFromStructureName(structureName: string) {
    return this.pathTemplates.structurePathTemplate.match(structureName).enterprise;
  }

  /**
   * Parse the structure from Structure resource.
   *
   * @param {string} structureName
   *   A fully-qualified path representing Structure resource.
   * @returns {string} A string representing the structure.
   */
  matchStructureFromStructureName(structureName: string) {
    return this.pathTemplates.structurePathTemplate.match(structureName).structure;
  }

  /**
   * Terminate the gRPC channel and close the client.
   *
   * The client will no longer be usable and all future behavior is undefined.
   * @returns {Promise} A promise that resolves when the client is closed.
   */
  close(): Promise<void> {
    this.initialize();
    if (!this._terminated) {
      return this.smartDeviceManagementServiceStub!.then(stub => {
        this._terminated = true;
        stub.close();
      });
    }
    return Promise.resolve();
  }
}
