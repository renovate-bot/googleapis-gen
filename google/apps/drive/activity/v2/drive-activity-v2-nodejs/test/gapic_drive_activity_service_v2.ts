// Copyright 2021 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// ** This file is automatically generated by gapic-generator-typescript. **
// ** https://github.com/googleapis/gapic-generator-typescript **
// ** All changes to this file may be overwritten. **

import * as protos from '../protos/protos';
import * as assert from 'assert';
import * as sinon from 'sinon';
import {SinonStub} from 'sinon';
import { describe, it } from 'mocha';
import * as driveactivityserviceModule from '../src';

import {PassThrough} from 'stream';

import {protobuf} from 'google-gax';

function generateSampleMessage<T extends object>(instance: T) {
    const filledObject = (instance.constructor as typeof protobuf.Message)
        .toObject(instance as protobuf.Message<T>, {defaults: true});
    return (instance.constructor as typeof protobuf.Message).fromObject(filledObject) as T;
}

function stubSimpleCall<ResponseType>(response?: ResponseType, error?: Error) {
    return error ? sinon.stub().rejects(error) : sinon.stub().resolves([response]);
}

function stubSimpleCallWithCallback<ResponseType>(response?: ResponseType, error?: Error) {
    return error ? sinon.stub().callsArgWith(2, error) : sinon.stub().callsArgWith(2, null, response);
}

function stubPageStreamingCall<ResponseType>(responses?: ResponseType[], error?: Error) {
    const pagingStub = sinon.stub();
    if (responses) {
        for (let i = 0; i < responses.length; ++i) {
            pagingStub.onCall(i).callsArgWith(2, null, responses[i]);
        }
    }
    const transformStub = error ? sinon.stub().callsArgWith(2, error) : pagingStub;
    const mockStream = new PassThrough({
        objectMode: true,
        transform: transformStub,
    });
    // trigger as many responses as needed
    if (responses) {
        for (let i = 0; i < responses.length; ++i) {
            setImmediate(() => { mockStream.write({}); });
        }
        setImmediate(() => { mockStream.end(); });
    } else {
        setImmediate(() => { mockStream.write({}); });
        setImmediate(() => { mockStream.end(); });
    }
    return sinon.stub().returns(mockStream);
}

function stubAsyncIterationCall<ResponseType>(responses?: ResponseType[], error?: Error) {
    let counter = 0;
    const asyncIterable = {
        [Symbol.asyncIterator]() {
            return {
                async next() {
                    if (error) {
                        return Promise.reject(error);
                    }
                    if (counter >= responses!.length) {
                        return Promise.resolve({done: true, value: undefined});
                    }
                    return Promise.resolve({done: false, value: responses![counter++]});
                }
            };
        }
    };
    return sinon.stub().returns(asyncIterable);
}

describe('v2.DriveActivityServiceClient', () => {
    it('has servicePath', () => {
        const servicePath = driveactivityserviceModule.v2.DriveActivityServiceClient.servicePath;
        assert(servicePath);
    });

    it('has apiEndpoint', () => {
        const apiEndpoint = driveactivityserviceModule.v2.DriveActivityServiceClient.apiEndpoint;
        assert(apiEndpoint);
    });

    it('has port', () => {
        const port = driveactivityserviceModule.v2.DriveActivityServiceClient.port;
        assert(port);
        assert(typeof port === 'number');
    });

    it('should create a client with no option', () => {
        const client = new driveactivityserviceModule.v2.DriveActivityServiceClient();
        assert(client);
    });

    it('should create a client with gRPC fallback', () => {
        const client = new driveactivityserviceModule.v2.DriveActivityServiceClient({
            fallback: true,
        });
        assert(client);
    });

    it('has initialize method and supports deferred initialization', async () => {
        const client = new driveactivityserviceModule.v2.DriveActivityServiceClient({
            credentials: { client_email: 'bogus', private_key: 'bogus' },
            projectId: 'bogus',
        });
        assert.strictEqual(client.driveActivityServiceStub, undefined);
        await client.initialize();
        assert(client.driveActivityServiceStub);
    });

    it('has close method', () => {
        const client = new driveactivityserviceModule.v2.DriveActivityServiceClient({
            credentials: { client_email: 'bogus', private_key: 'bogus' },
            projectId: 'bogus',
        });
        client.close();
    });

    it('has getProjectId method', async () => {
        const fakeProjectId = 'fake-project-id';
        const client = new driveactivityserviceModule.v2.DriveActivityServiceClient({
            credentials: { client_email: 'bogus', private_key: 'bogus' },
            projectId: 'bogus',
        });
        client.auth.getProjectId = sinon.stub().resolves(fakeProjectId);
        const result = await client.getProjectId();
        assert.strictEqual(result, fakeProjectId);
        assert((client.auth.getProjectId as SinonStub).calledWithExactly());
    });

    it('has getProjectId method with callback', async () => {
        const fakeProjectId = 'fake-project-id';
        const client = new driveactivityserviceModule.v2.DriveActivityServiceClient({
            credentials: { client_email: 'bogus', private_key: 'bogus' },
            projectId: 'bogus',
        });
        client.auth.getProjectId = sinon.stub().callsArgWith(0, null, fakeProjectId);
        const promise = new Promise((resolve, reject) => {
            client.getProjectId((err?: Error|null, projectId?: string|null) => {
                if (err) {
                    reject(err);
                } else {
                    resolve(projectId);
                }
            });
        });
        const result = await promise;
        assert.strictEqual(result, fakeProjectId);
    });

    describe('queryDriveActivity', () => {
        it('invokes queryDriveActivity without error', async () => {
            const client = new driveactivityserviceModule.v2.DriveActivityServiceClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.apps.drive.activity.v2.QueryDriveActivityRequest());
            const expectedOptions = {};
            const expectedResponse = [
              generateSampleMessage(new protos.google.apps.drive.activity.v2.DriveActivity()),
              generateSampleMessage(new protos.google.apps.drive.activity.v2.DriveActivity()),
              generateSampleMessage(new protos.google.apps.drive.activity.v2.DriveActivity()),
            ];
            client.innerApiCalls.queryDriveActivity = stubSimpleCall(expectedResponse);
            const [response] = await client.queryDriveActivity(request);
            assert.deepStrictEqual(response, expectedResponse);
            assert((client.innerApiCalls.queryDriveActivity as SinonStub)
                .getCall(0).calledWith(request, expectedOptions, undefined));
        });

        it('invokes queryDriveActivity without error using callback', async () => {
            const client = new driveactivityserviceModule.v2.DriveActivityServiceClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.apps.drive.activity.v2.QueryDriveActivityRequest());
            const expectedOptions = {};
            const expectedResponse = [
              generateSampleMessage(new protos.google.apps.drive.activity.v2.DriveActivity()),
              generateSampleMessage(new protos.google.apps.drive.activity.v2.DriveActivity()),
              generateSampleMessage(new protos.google.apps.drive.activity.v2.DriveActivity()),
            ];
            client.innerApiCalls.queryDriveActivity = stubSimpleCallWithCallback(expectedResponse);
            const promise = new Promise((resolve, reject) => {
                 client.queryDriveActivity(
                    request,
                    (err?: Error|null, result?: protos.google.apps.drive.activity.v2.IDriveActivity[]|null) => {
                        if (err) {
                            reject(err);
                        } else {
                            resolve(result);
                        }
                    });
            });
            const response = await promise;
            assert.deepStrictEqual(response, expectedResponse);
            assert((client.innerApiCalls.queryDriveActivity as SinonStub)
                .getCall(0).calledWith(request, expectedOptions /*, callback defined above */));
        });

        it('invokes queryDriveActivity with error', async () => {
            const client = new driveactivityserviceModule.v2.DriveActivityServiceClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.apps.drive.activity.v2.QueryDriveActivityRequest());
            const expectedOptions = {};
            const expectedError = new Error('expected');
            client.innerApiCalls.queryDriveActivity = stubSimpleCall(undefined, expectedError);
            await assert.rejects(client.queryDriveActivity(request), expectedError);
            assert((client.innerApiCalls.queryDriveActivity as SinonStub)
                .getCall(0).calledWith(request, expectedOptions, undefined));
        });

        it('invokes queryDriveActivityStream without error', async () => {
            const client = new driveactivityserviceModule.v2.DriveActivityServiceClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.apps.drive.activity.v2.QueryDriveActivityRequest());
            const expectedResponse = [
              generateSampleMessage(new protos.google.apps.drive.activity.v2.DriveActivity()),
              generateSampleMessage(new protos.google.apps.drive.activity.v2.DriveActivity()),
              generateSampleMessage(new protos.google.apps.drive.activity.v2.DriveActivity()),
            ];
            client.descriptors.page.queryDriveActivity.createStream = stubPageStreamingCall(expectedResponse);
            const stream = client.queryDriveActivityStream(request);
            const promise = new Promise((resolve, reject) => {
                const responses: protos.google.apps.drive.activity.v2.DriveActivity[] = [];
                stream.on('data', (response: protos.google.apps.drive.activity.v2.DriveActivity) => {
                    responses.push(response);
                });
                stream.on('end', () => {
                    resolve(responses);
                });
                stream.on('error', (err: Error) => {
                    reject(err);
                });
            });
            const responses = await promise;
            assert.deepStrictEqual(responses, expectedResponse);
            assert((client.descriptors.page.queryDriveActivity.createStream as SinonStub)
                .getCall(0).calledWith(client.innerApiCalls.queryDriveActivity, request));
        });

        it('invokes queryDriveActivityStream with error', async () => {
            const client = new driveactivityserviceModule.v2.DriveActivityServiceClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.apps.drive.activity.v2.QueryDriveActivityRequest());
            const expectedError = new Error('expected');
            client.descriptors.page.queryDriveActivity.createStream = stubPageStreamingCall(undefined, expectedError);
            const stream = client.queryDriveActivityStream(request);
            const promise = new Promise((resolve, reject) => {
                const responses: protos.google.apps.drive.activity.v2.DriveActivity[] = [];
                stream.on('data', (response: protos.google.apps.drive.activity.v2.DriveActivity) => {
                    responses.push(response);
                });
                stream.on('end', () => {
                    resolve(responses);
                });
                stream.on('error', (err: Error) => {
                    reject(err);
                });
            });
            await assert.rejects(promise, expectedError);
            assert((client.descriptors.page.queryDriveActivity.createStream as SinonStub)
                .getCall(0).calledWith(client.innerApiCalls.queryDriveActivity, request));
        });

        it('uses async iteration with queryDriveActivity without error', async () => {
            const client = new driveactivityserviceModule.v2.DriveActivityServiceClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.apps.drive.activity.v2.QueryDriveActivityRequest());const expectedResponse = [
              generateSampleMessage(new protos.google.apps.drive.activity.v2.DriveActivity()),
              generateSampleMessage(new protos.google.apps.drive.activity.v2.DriveActivity()),
              generateSampleMessage(new protos.google.apps.drive.activity.v2.DriveActivity()),
            ];
            client.descriptors.page.queryDriveActivity.asyncIterate = stubAsyncIterationCall(expectedResponse);
            const responses: protos.google.apps.drive.activity.v2.IDriveActivity[] = [];
            const iterable = client.queryDriveActivityAsync(request);
            for await (const resource of iterable) {
                responses.push(resource!);
            }
            assert.deepStrictEqual(responses, expectedResponse);
            assert.deepStrictEqual(
                (client.descriptors.page.queryDriveActivity.asyncIterate as SinonStub)
                    .getCall(0).args[1], request);
        });

        it('uses async iteration with queryDriveActivity with error', async () => {
            const client = new driveactivityserviceModule.v2.DriveActivityServiceClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.apps.drive.activity.v2.QueryDriveActivityRequest());const expectedError = new Error('expected');
            client.descriptors.page.queryDriveActivity.asyncIterate = stubAsyncIterationCall(undefined, expectedError);
            const iterable = client.queryDriveActivityAsync(request);
            await assert.rejects(async () => {
                const responses: protos.google.apps.drive.activity.v2.IDriveActivity[] = [];
                for await (const resource of iterable) {
                    responses.push(resource!);
                }
            });
            assert.deepStrictEqual(
                (client.descriptors.page.queryDriveActivity.asyncIterate as SinonStub)
                    .getCall(0).args[1], request);
        });
    });
});
