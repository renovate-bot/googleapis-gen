// Copyright 2021 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// ** This file is automatically generated by gapic-generator-typescript. **
// ** https://github.com/googleapis/gapic-generator-typescript **
// ** All changes to this file may be overwritten. **

import * as protos from '../protos/protos';
import * as assert from 'assert';
import * as sinon from 'sinon';
import {SinonStub} from 'sinon';
import { describe, it } from 'mocha';
import * as admobapiModule from '../src';

import {PassThrough} from 'stream';

import {protobuf} from 'google-gax';

function generateSampleMessage<T extends object>(instance: T) {
    const filledObject = (instance.constructor as typeof protobuf.Message)
        .toObject(instance as protobuf.Message<T>, {defaults: true});
    return (instance.constructor as typeof protobuf.Message).fromObject(filledObject) as T;
}

function stubSimpleCall<ResponseType>(response?: ResponseType, error?: Error) {
    return error ? sinon.stub().rejects(error) : sinon.stub().resolves([response]);
}

function stubSimpleCallWithCallback<ResponseType>(response?: ResponseType, error?: Error) {
    return error ? sinon.stub().callsArgWith(2, error) : sinon.stub().callsArgWith(2, null, response);
}

function stubServerStreamingCall<ResponseType>(response?: ResponseType, error?: Error) {
    const transformStub = error ? sinon.stub().callsArgWith(2, error) : sinon.stub().callsArgWith(2, null, response);
    const mockStream = new PassThrough({
        objectMode: true,
        transform: transformStub,
    });
    // write something to the stream to trigger transformStub and send the response back to the client
    setImmediate(() => { mockStream.write({}); });
    setImmediate(() => { mockStream.end(); });
    return sinon.stub().returns(mockStream);
}

function stubPageStreamingCall<ResponseType>(responses?: ResponseType[], error?: Error) {
    const pagingStub = sinon.stub();
    if (responses) {
        for (let i = 0; i < responses.length; ++i) {
            pagingStub.onCall(i).callsArgWith(2, null, responses[i]);
        }
    }
    const transformStub = error ? sinon.stub().callsArgWith(2, error) : pagingStub;
    const mockStream = new PassThrough({
        objectMode: true,
        transform: transformStub,
    });
    // trigger as many responses as needed
    if (responses) {
        for (let i = 0; i < responses.length; ++i) {
            setImmediate(() => { mockStream.write({}); });
        }
        setImmediate(() => { mockStream.end(); });
    } else {
        setImmediate(() => { mockStream.write({}); });
        setImmediate(() => { mockStream.end(); });
    }
    return sinon.stub().returns(mockStream);
}

function stubAsyncIterationCall<ResponseType>(responses?: ResponseType[], error?: Error) {
    let counter = 0;
    const asyncIterable = {
        [Symbol.asyncIterator]() {
            return {
                async next() {
                    if (error) {
                        return Promise.reject(error);
                    }
                    if (counter >= responses!.length) {
                        return Promise.resolve({done: true, value: undefined});
                    }
                    return Promise.resolve({done: false, value: responses![counter++]});
                }
            };
        }
    };
    return sinon.stub().returns(asyncIterable);
}

describe('v1.AdMobApiClient', () => {
    it('has servicePath', () => {
        const servicePath = admobapiModule.v1.AdMobApiClient.servicePath;
        assert(servicePath);
    });

    it('has apiEndpoint', () => {
        const apiEndpoint = admobapiModule.v1.AdMobApiClient.apiEndpoint;
        assert(apiEndpoint);
    });

    it('has port', () => {
        const port = admobapiModule.v1.AdMobApiClient.port;
        assert(port);
        assert(typeof port === 'number');
    });

    it('should create a client with no option', () => {
        const client = new admobapiModule.v1.AdMobApiClient();
        assert(client);
    });

    it('should create a client with gRPC fallback', () => {
        const client = new admobapiModule.v1.AdMobApiClient({
            fallback: true,
        });
        assert(client);
    });

    it('has initialize method and supports deferred initialization', async () => {
        const client = new admobapiModule.v1.AdMobApiClient({
              credentials: {client_email: 'bogus', private_key: 'bogus'},
              projectId: 'bogus',
        });
        assert.strictEqual(client.adMobApiStub, undefined);
        await client.initialize();
        assert(client.adMobApiStub);
    });

    it('has close method', () => {
        const client = new admobapiModule.v1.AdMobApiClient({
              credentials: {client_email: 'bogus', private_key: 'bogus'},
              projectId: 'bogus',
        });
        client.close();
    });

    it('has getProjectId method', async () => {
        const fakeProjectId = 'fake-project-id';
        const client = new admobapiModule.v1.AdMobApiClient({
              credentials: {client_email: 'bogus', private_key: 'bogus'},
              projectId: 'bogus',
        });
        client.auth.getProjectId = sinon.stub().resolves(fakeProjectId);
        const result = await client.getProjectId();
        assert.strictEqual(result, fakeProjectId);
        assert((client.auth.getProjectId as SinonStub).calledWithExactly());
    });

    it('has getProjectId method with callback', async () => {
        const fakeProjectId = 'fake-project-id';
        const client = new admobapiModule.v1.AdMobApiClient({
              credentials: {client_email: 'bogus', private_key: 'bogus'},
              projectId: 'bogus',
        });
        client.auth.getProjectId = sinon.stub().callsArgWith(0, null, fakeProjectId);
        const promise = new Promise((resolve, reject) => {
            client.getProjectId((err?: Error|null, projectId?: string|null) => {
                if (err) {
                    reject(err);
                } else {
                    resolve(projectId);
                }
            });
        });
        const result = await promise;
        assert.strictEqual(result, fakeProjectId);
    });

    describe('getPublisherAccount', () => {
        it('invokes getPublisherAccount without error', async () => {
            const client = new admobapiModule.v1.AdMobApiClient({
              credentials: {client_email: 'bogus', private_key: 'bogus'},
              projectId: 'bogus',
        });
            client.initialize();
            const request = generateSampleMessage(new protos.google.ads.admob.v1.GetPublisherAccountRequest());
            request.name = '';
            const expectedHeaderRequestParams = "name=";
            const expectedOptions = {
                otherArgs: {
                    headers: {
                        'x-goog-request-params': expectedHeaderRequestParams,
                    },
                },
            };
            const expectedResponse = generateSampleMessage(new protos.google.ads.admob.v1.PublisherAccount());
            client.innerApiCalls.getPublisherAccount = stubSimpleCall(expectedResponse);
            const [response] = await client.getPublisherAccount(request);
            assert.deepStrictEqual(response, expectedResponse);
            assert((client.innerApiCalls.getPublisherAccount as SinonStub)
                .getCall(0).calledWith(request, expectedOptions, undefined));
        });

        it('invokes getPublisherAccount without error using callback', async () => {
            const client = new admobapiModule.v1.AdMobApiClient({
              credentials: {client_email: 'bogus', private_key: 'bogus'},
              projectId: 'bogus',
        });
            client.initialize();
            const request = generateSampleMessage(new protos.google.ads.admob.v1.GetPublisherAccountRequest());
            request.name = '';
            const expectedHeaderRequestParams = "name=";
            const expectedOptions = {
                otherArgs: {
                    headers: {
                        'x-goog-request-params': expectedHeaderRequestParams,
                    },
                },
            };
            const expectedResponse = generateSampleMessage(new protos.google.ads.admob.v1.PublisherAccount());
            client.innerApiCalls.getPublisherAccount = stubSimpleCallWithCallback(expectedResponse);
            const promise = new Promise((resolve, reject) => {
                 client.getPublisherAccount(
                    request,
                    (err?: Error|null, result?: protos.google.ads.admob.v1.IPublisherAccount|null) => {
                        if (err) {
                            reject(err);
                        } else {
                            resolve(result);
                        }
                    });
            });
            const response = await promise;
            assert.deepStrictEqual(response, expectedResponse);
            assert((client.innerApiCalls.getPublisherAccount as SinonStub)
                .getCall(0).calledWith(request, expectedOptions /*, callback defined above */));
        });

        it('invokes getPublisherAccount with error', async () => {
            const client = new admobapiModule.v1.AdMobApiClient({
              credentials: {client_email: 'bogus', private_key: 'bogus'},
              projectId: 'bogus',
        });
            client.initialize();
            const request = generateSampleMessage(new protos.google.ads.admob.v1.GetPublisherAccountRequest());
            request.name = '';
            const expectedHeaderRequestParams = "name=";
            const expectedOptions = {
                otherArgs: {
                    headers: {
                        'x-goog-request-params': expectedHeaderRequestParams,
                    },
                },
            };
            const expectedError = new Error('expected');
            client.innerApiCalls.getPublisherAccount = stubSimpleCall(undefined, expectedError);
            await assert.rejects(client.getPublisherAccount(request), expectedError);
            assert((client.innerApiCalls.getPublisherAccount as SinonStub)
                .getCall(0).calledWith(request, expectedOptions, undefined));
        });
    });

    describe('generateNetworkReport', () => {
        it('invokes generateNetworkReport without error', async () => {
            const client = new admobapiModule.v1.AdMobApiClient({
              credentials: {client_email: 'bogus', private_key: 'bogus'},
              projectId: 'bogus',
        });
            client.initialize();
            const request = generateSampleMessage(new protos.google.ads.admob.v1.GenerateNetworkReportRequest());
            request.parent = '';
            const expectedHeaderRequestParams = "parent=";
            const expectedOptions = {
                otherArgs: {
                    headers: {
                        'x-goog-request-params': expectedHeaderRequestParams,
                    },
                },
            };
            const expectedResponse = generateSampleMessage(new protos.google.ads.admob.v1.GenerateNetworkReportResponse());
            client.innerApiCalls.generateNetworkReport = stubServerStreamingCall(expectedResponse);
            const stream = client.generateNetworkReport(request);
            const promise = new Promise((resolve, reject) => {
                stream.on('data', (response: protos.google.ads.admob.v1.GenerateNetworkReportResponse) => {
                    resolve(response);
                });
                stream.on('error', (err: Error) => {
                    reject(err);
                });
            });
            const response = await promise;
            assert.deepStrictEqual(response, expectedResponse);
            assert((client.innerApiCalls.generateNetworkReport as SinonStub)
                .getCall(0).calledWith(request, expectedOptions));
        });

        it('invokes generateNetworkReport with error', async () => {
            const client = new admobapiModule.v1.AdMobApiClient({
              credentials: {client_email: 'bogus', private_key: 'bogus'},
              projectId: 'bogus',
        });
            client.initialize();
            const request = generateSampleMessage(new protos.google.ads.admob.v1.GenerateNetworkReportRequest());
            request.parent = '';
            const expectedHeaderRequestParams = "parent=";
            const expectedOptions = {
                otherArgs: {
                    headers: {
                        'x-goog-request-params': expectedHeaderRequestParams,
                    },
                },
            };
            const expectedError = new Error('expected');
            client.innerApiCalls.generateNetworkReport = stubServerStreamingCall(undefined, expectedError);
            const stream = client.generateNetworkReport(request);
            const promise = new Promise((resolve, reject) => {
                stream.on('data', (response: protos.google.ads.admob.v1.GenerateNetworkReportResponse) => {
                    resolve(response);
                });
                stream.on('error', (err: Error) => {
                    reject(err);
                });
            });
            await assert.rejects(promise, expectedError);
            assert((client.innerApiCalls.generateNetworkReport as SinonStub)
                .getCall(0).calledWith(request, expectedOptions));
        });
    });

    describe('generateMediationReport', () => {
        it('invokes generateMediationReport without error', async () => {
            const client = new admobapiModule.v1.AdMobApiClient({
              credentials: {client_email: 'bogus', private_key: 'bogus'},
              projectId: 'bogus',
        });
            client.initialize();
            const request = generateSampleMessage(new protos.google.ads.admob.v1.GenerateMediationReportRequest());
            request.parent = '';
            const expectedHeaderRequestParams = "parent=";
            const expectedOptions = {
                otherArgs: {
                    headers: {
                        'x-goog-request-params': expectedHeaderRequestParams,
                    },
                },
            };
            const expectedResponse = generateSampleMessage(new protos.google.ads.admob.v1.GenerateMediationReportResponse());
            client.innerApiCalls.generateMediationReport = stubServerStreamingCall(expectedResponse);
            const stream = client.generateMediationReport(request);
            const promise = new Promise((resolve, reject) => {
                stream.on('data', (response: protos.google.ads.admob.v1.GenerateMediationReportResponse) => {
                    resolve(response);
                });
                stream.on('error', (err: Error) => {
                    reject(err);
                });
            });
            const response = await promise;
            assert.deepStrictEqual(response, expectedResponse);
            assert((client.innerApiCalls.generateMediationReport as SinonStub)
                .getCall(0).calledWith(request, expectedOptions));
        });

        it('invokes generateMediationReport with error', async () => {
            const client = new admobapiModule.v1.AdMobApiClient({
              credentials: {client_email: 'bogus', private_key: 'bogus'},
              projectId: 'bogus',
        });
            client.initialize();
            const request = generateSampleMessage(new protos.google.ads.admob.v1.GenerateMediationReportRequest());
            request.parent = '';
            const expectedHeaderRequestParams = "parent=";
            const expectedOptions = {
                otherArgs: {
                    headers: {
                        'x-goog-request-params': expectedHeaderRequestParams,
                    },
                },
            };
            const expectedError = new Error('expected');
            client.innerApiCalls.generateMediationReport = stubServerStreamingCall(undefined, expectedError);
            const stream = client.generateMediationReport(request);
            const promise = new Promise((resolve, reject) => {
                stream.on('data', (response: protos.google.ads.admob.v1.GenerateMediationReportResponse) => {
                    resolve(response);
                });
                stream.on('error', (err: Error) => {
                    reject(err);
                });
            });
            await assert.rejects(promise, expectedError);
            assert((client.innerApiCalls.generateMediationReport as SinonStub)
                .getCall(0).calledWith(request, expectedOptions));
        });
    });

    describe('listPublisherAccounts', () => {
        it('invokes listPublisherAccounts without error', async () => {
            const client = new admobapiModule.v1.AdMobApiClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.ads.admob.v1.ListPublisherAccountsRequest());
            const expectedOptions = {otherArgs: {headers: {}}};;
            const expectedResponse = [
              generateSampleMessage(new protos.google.ads.admob.v1.PublisherAccount()),
              generateSampleMessage(new protos.google.ads.admob.v1.PublisherAccount()),
              generateSampleMessage(new protos.google.ads.admob.v1.PublisherAccount()),
            ];
            client.innerApiCalls.listPublisherAccounts = stubSimpleCall(expectedResponse);
            const [response] = await client.listPublisherAccounts(request);
            assert.deepStrictEqual(response, expectedResponse);
            assert((client.innerApiCalls.listPublisherAccounts as SinonStub)
                .getCall(0).calledWith(request, expectedOptions, undefined));
        });

        it('invokes listPublisherAccounts without error using callback', async () => {
            const client = new admobapiModule.v1.AdMobApiClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.ads.admob.v1.ListPublisherAccountsRequest());
            const expectedOptions = {otherArgs: {headers: {}}};;
            const expectedResponse = [
              generateSampleMessage(new protos.google.ads.admob.v1.PublisherAccount()),
              generateSampleMessage(new protos.google.ads.admob.v1.PublisherAccount()),
              generateSampleMessage(new protos.google.ads.admob.v1.PublisherAccount()),
            ];
            client.innerApiCalls.listPublisherAccounts = stubSimpleCallWithCallback(expectedResponse);
            const promise = new Promise((resolve, reject) => {
                 client.listPublisherAccounts(
                    request,
                    (err?: Error|null, result?: protos.google.ads.admob.v1.IPublisherAccount[]|null) => {
                        if (err) {
                            reject(err);
                        } else {
                            resolve(result);
                        }
                    });
            });
            const response = await promise;
            assert.deepStrictEqual(response, expectedResponse);
            assert((client.innerApiCalls.listPublisherAccounts as SinonStub)
                .getCall(0).calledWith(request, expectedOptions /*, callback defined above */));
        });

        it('invokes listPublisherAccounts with error', async () => {
            const client = new admobapiModule.v1.AdMobApiClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.ads.admob.v1.ListPublisherAccountsRequest());
            const expectedOptions = {otherArgs: {headers: {}}};;
            const expectedError = new Error('expected');
            client.innerApiCalls.listPublisherAccounts = stubSimpleCall(undefined, expectedError);
            await assert.rejects(client.listPublisherAccounts(request), expectedError);
            assert((client.innerApiCalls.listPublisherAccounts as SinonStub)
                .getCall(0).calledWith(request, expectedOptions, undefined));
        });

        it('invokes listPublisherAccountsStream without error', async () => {
            const client = new admobapiModule.v1.AdMobApiClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.ads.admob.v1.ListPublisherAccountsRequest());
            const expectedResponse = [
              generateSampleMessage(new protos.google.ads.admob.v1.PublisherAccount()),
              generateSampleMessage(new protos.google.ads.admob.v1.PublisherAccount()),
              generateSampleMessage(new protos.google.ads.admob.v1.PublisherAccount()),
            ];
            client.descriptors.page.listPublisherAccounts.createStream = stubPageStreamingCall(expectedResponse);
            const stream = client.listPublisherAccountsStream(request);
            const promise = new Promise((resolve, reject) => {
                const responses: protos.google.ads.admob.v1.PublisherAccount[] = [];
                stream.on('data', (response: protos.google.ads.admob.v1.PublisherAccount) => {
                    responses.push(response);
                });
                stream.on('end', () => {
                    resolve(responses);
                });
                stream.on('error', (err: Error) => {
                    reject(err);
                });
            });
            const responses = await promise;
            assert.deepStrictEqual(responses, expectedResponse);
            assert((client.descriptors.page.listPublisherAccounts.createStream as SinonStub)
                .getCall(0).calledWith(client.innerApiCalls.listPublisherAccounts, request));
        });

        it('invokes listPublisherAccountsStream with error', async () => {
            const client = new admobapiModule.v1.AdMobApiClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.ads.admob.v1.ListPublisherAccountsRequest());
            const expectedError = new Error('expected');
            client.descriptors.page.listPublisherAccounts.createStream = stubPageStreamingCall(undefined, expectedError);
            const stream = client.listPublisherAccountsStream(request);
            const promise = new Promise((resolve, reject) => {
                const responses: protos.google.ads.admob.v1.PublisherAccount[] = [];
                stream.on('data', (response: protos.google.ads.admob.v1.PublisherAccount) => {
                    responses.push(response);
                });
                stream.on('end', () => {
                    resolve(responses);
                });
                stream.on('error', (err: Error) => {
                    reject(err);
                });
            });
            await assert.rejects(promise, expectedError);
            assert((client.descriptors.page.listPublisherAccounts.createStream as SinonStub)
                .getCall(0).calledWith(client.innerApiCalls.listPublisherAccounts, request));
        });

        it('uses async iteration with listPublisherAccounts without error', async () => {
            const client = new admobapiModule.v1.AdMobApiClient({
              credentials: {client_email: 'bogus', private_key: 'bogus'},
              projectId: 'bogus',
        });
            client.initialize();
            const request = generateSampleMessage(new protos.google.ads.admob.v1.ListPublisherAccountsRequest());
            const expectedResponse = [
              generateSampleMessage(new protos.google.ads.admob.v1.PublisherAccount()),
              generateSampleMessage(new protos.google.ads.admob.v1.PublisherAccount()),
              generateSampleMessage(new protos.google.ads.admob.v1.PublisherAccount()),
            ];
            client.descriptors.page.listPublisherAccounts.asyncIterate = stubAsyncIterationCall(expectedResponse);
            const responses: protos.google.ads.admob.v1.IPublisherAccount[] = [];
            const iterable = client.listPublisherAccountsAsync(request);
            for await (const resource of iterable) {
                responses.push(resource!);
            }
            assert.deepStrictEqual(responses, expectedResponse);
            assert.deepStrictEqual(
                (client.descriptors.page.listPublisherAccounts.asyncIterate as SinonStub)
                    .getCall(0).args[1], request);
        });

        it('uses async iteration with listPublisherAccounts with error', async () => {
            const client = new admobapiModule.v1.AdMobApiClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            const request = generateSampleMessage(new protos.google.ads.admob.v1.ListPublisherAccountsRequest());const expectedError = new Error('expected');
            client.descriptors.page.listPublisherAccounts.asyncIterate = stubAsyncIterationCall(undefined, expectedError);
            const iterable = client.listPublisherAccountsAsync(request);
            await assert.rejects(async () => {
                const responses: protos.google.ads.admob.v1.IPublisherAccount[] = [];
                for await (const resource of iterable) {
                    responses.push(resource!);
                }
            });
            assert.deepStrictEqual(
                (client.descriptors.page.listPublisherAccounts.asyncIterate as SinonStub)
                    .getCall(0).args[1], request);
        });
    });

    describe('Path templates', () => {

        describe('publisherAccount', () => {
            const fakePath = "/rendered/path/publisherAccount";
            const expectedParameters = {
                publisher: "publisherValue",
            };
            const client = new admobapiModule.v1.AdMobApiClient({
                credentials: {client_email: 'bogus', private_key: 'bogus'},
                projectId: 'bogus',
            });
            client.initialize();
            client.pathTemplates.publisherAccountPathTemplate.render =
                sinon.stub().returns(fakePath);
            client.pathTemplates.publisherAccountPathTemplate.match =
                sinon.stub().returns(expectedParameters);

            it('publisherAccountPath', () => {
                const result = client.publisherAccountPath("publisherValue");
                assert.strictEqual(result, fakePath);
                assert((client.pathTemplates.publisherAccountPathTemplate.render as SinonStub)
                    .getCall(-1).calledWith(expectedParameters));
            });

            it('matchPublisherFromPublisherAccountName', () => {
                const result = client.matchPublisherFromPublisherAccountName(fakePath);
                assert.strictEqual(result, "publisherValue");
                assert((client.pathTemplates.publisherAccountPathTemplate.match as SinonStub)
                    .getCall(-1).calledWith(fakePath));
            });
        });
    });
});
